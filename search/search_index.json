{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"django-grpc-extra","text":"<p><code>django-grpc-extra</code> helps you build gRPC APIs in Django with a decorator-first workflow.</p>"},{"location":"#what-you-get","title":"What You Get","text":"<ul> <li>Decorators for services and RPC methods</li> <li>Proto generation from Pydantic schemas</li> <li>Runtime conversion (<code>pb2 &lt;-&gt; pydantic</code>) and exception mapping</li> <li>Built-in server launcher with autodiscovery, health, reflection, reload</li> <li><code>ModelService</code> for CRUD-heavy endpoints</li> </ul>"},{"location":"#current-status","title":"Current Status","text":"<p>The project is in Beta.</p>"},{"location":"#read-this-first","title":"Read This First","text":"<ol> <li>Quickstart</li> <li>Core Decorators</li> <li>Authentication</li> <li>Permissions</li> </ol>"},{"location":"#compatibility","title":"Compatibility","text":"<ul> <li>Python 3.10+</li> <li>Django 3.2+</li> </ul>"},{"location":"#next-steps","title":"Next Steps","text":"<ul> <li>Start with one unary RPC and generate <code>.proto</code></li> <li>Enable health/reflection only when needed</li> <li>Add auth backend and permissions after basic smoke tests</li> </ul>"},{"location":"faq/","title":"FAQ","text":""},{"location":"faq/#why-do-i-get-unauthenticated-before-my-authenticate-method-runs","title":"Why do I get <code>UNAUTHENTICATED</code> before my <code>authenticate(...)</code> method runs?","text":"<p>Most often token extraction failed before custom auth logic:</p> <ul> <li>wrong metadata key</li> <li>wrong scheme prefix</li> <li>empty token value</li> </ul> <p>What to do:</p> <ol> <li>log incoming metadata keys in auth backend</li> <li>verify <code>GrpcBearerAuthBase.header</code> and <code>scheme</code></li> <li>confirm client sends metadata for the same method call</li> </ol>"},{"location":"faq/#how-do-i-allow-health-checks-without-authentication","title":"How do I allow health checks without authentication?","text":"<p>Whitelist health RPC methods in backend:</p> <ul> <li><code>/grpc.health.v1.Health/Check</code></li> <li><code>/grpc.health.v1.Health/Watch</code></li> </ul> <p>Return <code>True</code> for those methods before token validation.</p>"},{"location":"faq/#why-does-client-show-invalid-wire-type-parsing-error","title":"Why does client show <code>invalid wire type</code> / parsing error?","text":"<p>Usually proto contract mismatch between server and client cache.</p> <p>What to do:</p> <ol> <li>regenerate proto/pb2</li> <li>restart server</li> <li>recreate gRPC connection in client tool</li> <li>verify reflection is enabled if client relies on reflection</li> </ol>"},{"location":"faq/#why-does-search-not-change-results","title":"Why does search not change results?","text":"<p><code>grpc_searching</code> requires explicit fields.</p> <p>Use:</p> <pre><code>@grpc_searching(search_fields=[\"name\", \"sku\"])\n</code></pre> <p>Without fields, decorator raises error or search does nothing depending on configuration/class contract.</p>"},{"location":"faq/#why-does-ordering-fail-with-invalid-field-errors","title":"Why does ordering fail with invalid field errors?","text":"<p>Ordering validates fields against configured/available fields.</p> <p>What to do:</p> <ol> <li>pass explicit <code>ordering_fields</code></li> <li>ensure response items expose those fields (<code>dict</code> keys/object attrs)</li> <li>for queryset ordering, ensure field exists in model/annotation</li> </ol>"},{"location":"faq/#why-do-i-get-failed-to-encode-response","title":"Why do I get <code>Failed to encode response</code>?","text":"<p>Response payload doesn't match <code>response_schema</code> / proto shape.</p> <p>Typical causes:</p> <ul> <li>returned object has missing fields</li> <li>type mismatch (<code>Decimal</code>, date/time, nested objects)</li> <li>wrong top-level shape for unary response</li> </ul> <p>What to do:</p> <ol> <li>compare method return payload with schema</li> <li>verify generated proto message fields</li> <li>simplify response to minimal working payload, then add fields back</li> </ol>"},{"location":"faq/#can-unary-rpc-return-listschema","title":"Can unary RPC return <code>list[Schema]</code>?","text":"<p>Yes. Use:</p> <pre><code>@grpc_method(response_schema=list[ItemSchema])\n</code></pre> <p>Runtime wraps returned collection into internal <code>items</code> field.</p>"},{"location":"faq/#do-i-need-to-wrap-pagination-response-schema-manually","title":"Do I need to wrap pagination response schema manually?","text":"<p>No. With <code>@grpc_pagination</code> or <code>@grpc_pagination()</code> you pass item schema only.</p> <p>Pagination decorator augments request/response schemas automatically.</p>"},{"location":"faq/#why-does-pre-commit-pass-manually-but-fail-on-commit","title":"Why does pre-commit pass manually but fail on commit?","text":"<p>Usually different environment/path context (e.g. missing activated virtualenv in commit hook shell).</p> <p>What to do:</p> <ol> <li>ensure tools (<code>ruff</code>, <code>mypy</code>, etc.) are installed in environment used by hooks</li> <li>run <code>pre-commit run --all-files</code> in the same shell context used for git commit</li> <li>avoid relying on shell-local state not available in hook subprocess</li> </ol>"},{"location":"faq/#what-does-modulenotfounderror-name_pb2-mean","title":"What does <code>ModuleNotFoundError: &lt;name&gt;_pb2</code> mean?","text":"<p>Generated pb2 files are missing or generated into wrong include/output path.</p> <p>Fix:</p> <ul> <li>run <code>generate_proto</code> again</li> <li>verify app root and proto import/include paths</li> </ul>"},{"location":"faq/#why-reflection-doesnt-show-my-service","title":"Why reflection doesn't show my service?","text":"<p>Possible causes:</p> <ul> <li>reflection disabled</li> <li>service not discovered/registered</li> <li>stale client cache</li> </ul> <p>Check:</p> <ol> <li>run with <code>--reflection</code></li> <li>inspect server startup logs for discovered/registered services</li> <li>recreate client connection</li> </ol>"},{"location":"installation/","title":"Installation","text":"<p>Base package:</p> <pre><code>pip install django-grpc-extra\n</code></pre> <p>Optional extras:</p> <pre><code>pip install \"django-grpc-extra[codegen]\"\npip install \"django-grpc-extra[health]\"\npip install \"django-grpc-extra[reflection]\"\npip install \"django-grpc-extra[reload]\"\n</code></pre> <p>Development install:</p> <pre><code>pip install -e \".[dev,codegen,health,reflection,reload]\"\n</code></pre>"},{"location":"quickstart/","title":"Quickstart","text":""},{"location":"quickstart/#1-install","title":"1) Install","text":"<pre><code>pip install \"django-grpc-extra[codegen]\"\n</code></pre>"},{"location":"quickstart/#2-add-app","title":"2) Add app","text":"<pre><code>INSTALLED_APPS = [\n    ...,\n    \"grpc_extra\",\n]\n</code></pre>"},{"location":"quickstart/#3-init-grpc-layout","title":"3) Init grpc layout","text":"<pre><code>python -m django init_grpc --app products\n</code></pre>"},{"location":"quickstart/#4-add-service","title":"4) Add service","text":"<pre><code>from pydantic import BaseModel\nfrom grpc_extra import grpc_method, grpc_service\n\n\nclass PingRequest(BaseModel):\n    message: str\n\n\nclass PingResponse(BaseModel):\n    message: str\n\n\n@grpc_service(app_label=\"products\", package=\"products\")\nclass ExampleService:\n    @grpc_method(request_schema=PingRequest, response_schema=PingResponse)\n    def ping(self, request, context):\n        return {\"message\": f\"pong: {request.message}\"}\n</code></pre>"},{"location":"quickstart/#5-generate-protopb2","title":"5) Generate proto/pb2","text":"<pre><code>python -m django generate_proto --app products\n</code></pre>"},{"location":"quickstart/#6-run-server","title":"6) Run server","text":"<pre><code>python manage.py run_grpcserver --health --reflection\n</code></pre>"},{"location":"quickstart/#7-invoke","title":"7) Invoke","text":"<p>Use grpcurl/Postman/BloomRPC and call:</p> <ul> <li>service: <code>ExampleService</code></li> <li>method: <code>Ping</code></li> </ul> <p>Payload example:</p> <pre><code>{\"message\": \"hello\"}\n</code></pre>"},{"location":"commands/generate-client-sdk/","title":"Command: <code>generate_client_sdk</code>","text":"<p>Generate client SDK from discovered gRPC proto contracts.</p> <p>Status: Experimental</p> <p>SDK generator API and generated file layout may change between releases.</p> <p>Basic usage:</p> <pre><code>python manage.py generate_client_sdk --language python --all\n</code></pre>"},{"location":"commands/generate-client-sdk/#what-command-does","title":"What Command Does","text":"<ol> <li>discovers registered gRPC services</li> <li>ensures proto files are generated (unless <code>--skip-proto</code>)</li> <li>runs selected SDK generator</li> <li>writes SDK package to output directory</li> </ol>"},{"location":"commands/generate-client-sdk/#options","title":"Options","text":"<ul> <li><code>--language</code> (required): target SDK language</li> <li><code>--out</code>: output directory (default: <code>generated-sdks</code>)</li> <li><code>--name</code>: SDK package/folder name</li> <li><code>--app &lt;label&gt;</code>: include app (repeatable)</li> <li><code>--all</code>: include all installed apps</li> <li><code>--skip-proto</code>: skip proto regeneration and use existing proto files</li> </ul> <p>Examples:</p> <pre><code># python sdk for all apps\npython manage.py generate_client_sdk --language python --all\n\n# python sdk for one app\npython manage.py generate_client_sdk --language python --app products --name products-grpc-sdk\n\n# generate to custom location\npython manage.py generate_client_sdk --language python --all --out ./artifacts\n\n# use existing proto files only\npython manage.py generate_client_sdk --language python --all --skip-proto\n</code></pre>"},{"location":"commands/generate-client-sdk/#python-sdk-layout-current-experimental","title":"Python SDK Layout (Current Experimental)","text":"<pre><code>&lt;sdk&gt;/src/&lt;package&gt;/\n  &lt;app&gt;/grpc/proto/            # generated protobuf artifacts\n    *_pb2.py\n    *_pb2_grpc.py\n    *.pyi\n  client.py                  # custom layer (not overwritten if exists)\n  client_generated.py        # regenerated\n  helpers.py                 # helper module (created if missing, not overwritten)\n  services.py                # facade, regenerated\n  typed_services.py          # facade, regenerated\n  models.py                  # facade, regenerated\n  generated/\n    &lt;app&gt;/\n      services.py            # regenerated\n      typed_services.py      # regenerated\n      models.py              # regenerated\n</code></pre> <p>Regeneration behavior:</p> <ul> <li>regenerated every run: generated facades and <code>generated/&lt;app&gt;/...</code></li> <li>preserved if exists: <code>client.py</code>, <code>helpers.py</code></li> </ul>"},{"location":"commands/generate-client-sdk/#raw-and-typed-layers","title":"Raw and Typed Layers","text":"<p>Python SDK exposes two access patterns:</p> <ul> <li>raw protobuf layer: <code>client.&lt;service&gt;.&lt;method&gt;()</code></li> <li>typed pydantic layer: <code>client.typed.&lt;service&gt;.&lt;method&gt;()</code></li> </ul> <pre><code>from my_sdk import ClientConfig, GrpcClient, extract_results, message_to_dict\n\nclient = GrpcClient(ClientConfig(host=\"localhost:50051\"))\n\nraw_response = client.product.list({\"limit\": 20})\nraw_payload = message_to_dict(raw_response)\nrows = extract_results(raw_response)\n\ntyped_response = client.typed.product.list({\"limit\": 20})\n</code></pre>"},{"location":"commands/generate-client-sdk/#custom-generators","title":"Custom Generators","text":"<p>You can register custom generators via settings:</p> <pre><code>GRPC_EXTRA = {\n    \"SDK_GENERATORS\": {\n        \"python\": \"grpc_extra.sdk.generators.PythonClientSDKGenerator\",\n        \"php\": \"grpc_extra.sdk.generators.PhpClientSDKGenerator\",\n        \"my_lang\": \"path.to.MyGenerator\",\n    }\n}\n</code></pre> <p>Generator must inherit <code>BaseClientSDKGenerator</code>.</p>"},{"location":"commands/generate-client-sdk/#common-errors","title":"Common Errors","text":""},{"location":"commands/generate-client-sdk/#apps-have-different-include-roots","title":"<code>Apps have different include roots</code>","text":"<p>Run command per app:</p> <pre><code>python manage.py generate_client_sdk --language python --app app_one\npython manage.py generate_client_sdk --language python --app app_two\n</code></pre>"},{"location":"commands/generate-client-sdk/#grpcio-tools-is-required","title":"<code>grpcio-tools is required</code>","text":"<p>Install extra dependencies:</p> <pre><code>pip install \"django-grpc-extra[codegen,sdk]\"\n</code></pre>"},{"location":"commands/generate-client-sdk/#generated-sdk-mismatch-with-server","title":"Generated SDK mismatch with server","text":"<p>Regenerate proto and SDK from the same revision, then reinstall SDK artifact.</p>"},{"location":"commands/generate-client-sdk/#recommendation","title":"Recommendation","text":"<p>Treat this command as an accelerator for internal SDK workflows. For external/public SDK distribution, pin exact SDK versions and test generated artifacts in CI.</p>"},{"location":"commands/generate-proto/","title":"Command: <code>generate_proto</code>","text":"<p>Generates <code>.proto</code> files from registered gRPC services and (optionally) compiles Python stubs (<code>*_pb2.py</code>, <code>*_pb2_grpc.py</code>, and optionally <code>*.pyi</code>).</p> <p>Basic usage:</p> <pre><code>python manage.py generate_proto --app &lt;app_label&gt;\n</code></pre>"},{"location":"commands/generate-proto/#what-command-does","title":"What Command Does","text":"<ol> <li>auto-discovers services (<code>{app}.grpc.services</code> by default)</li> <li>builds proto definitions from method schemas</li> <li>writes/updates app proto files</li> <li>optionally compiles proto to Python stubs</li> </ol>"},{"location":"commands/generate-proto/#options","title":"Options","text":"<ul> <li><code>--app &lt;label&gt;</code>: process only selected app; can be repeated</li> <li><code>--all</code>: process all installed apps</li> <li><code>--force</code>: rewrite proto files even if content changed detection would skip</li> <li><code>--no-compile</code>: only write <code>.proto</code>, skip <code>pb2</code> generation</li> <li><code>--pyi</code>: generate <code>*.pyi</code> type stubs during compile</li> </ul> <p>Recommended patterns:</p> <pre><code># one app\npython manage.py generate_proto --app products\n\n# all apps\npython manage.py generate_proto --all\n\n# regenerate and compile with type stubs\npython manage.py generate_proto --app products --force --pyi\n\n# proto only (for CI diff checks, for example)\npython manage.py generate_proto --app products --no-compile\n</code></pre>"},{"location":"commands/generate-proto/#typical-workflow","title":"Typical Workflow","text":"<p>When service/schema code changed:</p> <ol> <li>run <code>generate_proto</code></li> <li>commit updated <code>.proto</code> and generated stubs</li> <li>restart gRPC server</li> <li>refresh client descriptors in tooling (Postman/BloomRPC/etc.)</li> </ol>"},{"location":"commands/generate-proto/#multi-app-root-limitation","title":"Multi-app Root Limitation","text":"<p>If apps are discovered under different include roots, command can raise:</p> <p><code>Apps have different include roots; use --app for a single app root.</code></p> <p>In that case, run command per app:</p> <pre><code>python manage.py generate_proto --app app_one\npython manage.py generate_proto --app app_two\n</code></pre>"},{"location":"commands/generate-proto/#common-errors","title":"Common Errors","text":""},{"location":"commands/generate-proto/#googleprotobufemptyproto-file-not-found","title":"<code>google/protobuf/empty.proto: File not found</code>","text":"<p>Reason: protoc include path for bundled Google protos not resolved in environment.</p> <p>What to check:</p> <ol> <li><code>grpcio-tools</code> is installed in active venv</li> <li>command is run from the same Python environment</li> <li>library version includes fixed protoc include resolution</li> </ol>"},{"location":"commands/generate-proto/#attributeerror-type-object-list-has-no-attribute-model_fields","title":"<code>AttributeError: type object 'list' has no attribute 'model_fields'</code>","text":"<p>Reason: old generation path expected model class while method used top-level <code>list[T]</code> response schema.</p> <p>Expected behavior in current implementation: list response schemas are supported and wrapped into repeated message payload.</p>"},{"location":"commands/generate-proto/#proto-name-conflicts","title":"Proto name conflicts","text":"<p>Example: duplicate message names for combined decorators/endpoints.</p> <p>Fix:</p> <ol> <li>ensure latest library version with message name dedup logic</li> <li>avoid manually forcing identical explicit message names across incompatible schemas</li> </ol>"},{"location":"commands/generate-proto/#best-practices","title":"Best Practices","text":"<ol> <li>Use <code>--app</code> in local development for faster iteration.</li> <li>Use <code>--all</code> in CI/release checks.</li> <li>Keep generated files committed, so server and clients stay in sync.</li> <li>Use <code>--pyi</code> if team relies on IDE/static typing for generated stubs.</li> <li>Regenerate after changing decorators that alter request/response envelopes (<code>pagination</code>, <code>searching</code>, <code>ordering</code>).</li> </ol>"},{"location":"commands/init-grpc/","title":"Command: <code>init_grpc</code>","text":"<p>Initializes base gRPC scaffold inside Django app(s).</p> <p>Basic usage:</p> <pre><code>python manage.py init_grpc --app products\n</code></pre> <p>If no flags are provided, command behaves as <code>--all</code>.</p>"},{"location":"commands/init-grpc/#what-command-creates","title":"What Command Creates","text":"<p>For each target app it creates:</p> <ul> <li><code>&lt;app&gt;/grpc/__init__.py</code></li> <li><code>&lt;app&gt;/grpc/services.py</code></li> <li><code>&lt;app&gt;/grpc/proto/__init__.py</code></li> </ul> <p>These files are the minimal structure required to start defining services and generating proto.</p>"},{"location":"commands/init-grpc/#options","title":"Options","text":"<ul> <li><code>--app &lt;label&gt;</code>: target app label; can be repeated</li> <li><code>--all</code>: scaffold every installed app</li> <li><code>--force</code>: overwrite existing scaffold files</li> </ul> <p>Examples:</p> <pre><code># single app\npython manage.py init_grpc --app products\n\n# multiple explicit apps\npython manage.py init_grpc --app products --app billing\n\n# all installed apps\npython manage.py init_grpc --all\n\n# rewrite existing files\npython manage.py init_grpc --app products --force\n</code></pre>"},{"location":"commands/init-grpc/#output-behavior","title":"Output Behavior","text":"<p>Command prints one line per file:</p> <ul> <li><code>write &lt;path&gt;</code> when file is created/overwritten</li> <li><code>skip &lt;path&gt;</code> when file exists and <code>--force</code> is not set</li> </ul> <p>Final summary:</p> <ul> <li><code>gRPC scaffold complete. created=&lt;N&gt; skipped=&lt;M&gt;</code></li> </ul>"},{"location":"commands/init-grpc/#validation-rules","title":"Validation Rules","text":"<ul> <li><code>--all</code> and <code>--app</code> cannot be used together.</li> <li>Unknown app labels produce <code>CommandError</code>.</li> <li>If no matching apps found, command fails.</li> </ul> <p>Typical errors:</p> <ul> <li><code>Use either --all or --app, not both.</code></li> <li><code>Unknown app label(s): ...</code></li> <li><code>No matching apps found.</code></li> </ul>"},{"location":"commands/init-grpc/#recommended-workflow","title":"Recommended Workflow","text":"<ol> <li>Run <code>init_grpc</code> for your app.</li> <li>Implement services in <code>&lt;app&gt;/grpc/services.py</code>.</li> <li>Run <code>generate_proto</code>.</li> <li>Start server with <code>run_grpcserver</code>.</li> </ol> <p>Example:</p> <pre><code>python manage.py init_grpc --app products\npython manage.py generate_proto --app products --force\npython manage.py run_grpcserver --bind 0.0.0.0:50051 --health --reflection\n</code></pre>"},{"location":"commands/init-grpc/#notes-on-force","title":"Notes on <code>--force</code>","text":"<p><code>--force</code> overwrites scaffold files, so use it carefully if those files were already customized.</p> <p>Safe approach:</p> <ol> <li>commit changes before rerunning with <code>--force</code></li> <li>rerun command</li> <li>restore/merge your custom code if needed</li> </ol>"},{"location":"commands/run-grpcserver/","title":"Command: <code>run_grpcserver</code>","text":"<p>Starts gRPC server for discovered services.</p> <p>Basic usage:</p> <pre><code>python manage.py run_grpcserver --bind [::]:50051 --health --reflection\n</code></pre>"},{"location":"commands/run-grpcserver/#what-command-does","title":"What Command Does","text":"<ol> <li>auto-discovers registered services</li> <li>builds runtime adapters for methods/schemas</li> <li>starts gRPC server with configured workers and interceptors</li> <li>optionally enables health/reflection and reload watcher</li> </ol>"},{"location":"commands/run-grpcserver/#options","title":"Options","text":"<ul> <li><code>--bind &lt;host:port&gt;</code>: bind address (default from command settings)</li> <li><code>--max-workers &lt;int&gt;</code>: thread pool workers</li> <li><code>--max-msg-mb &lt;int&gt;</code>: max request/response message size in MB</li> <li><code>--health</code> / <code>--no-health</code>: enable/disable gRPC health service</li> <li><code>--reflection</code> / <code>--no-reflection</code>: enable/disable server reflection</li> <li><code>--reload</code> / <code>--no-reload</code>: enable/disable live reload watcher</li> <li><code>--reload-path &lt;path&gt;</code>: additional watched path; can be repeated</li> <li><code>--auth-backend &lt;import.path&gt;</code>: auth backend class/function path</li> <li><code>--interceptor &lt;import.path&gt;</code>: extra interceptor; can be repeated</li> <li><code>--request-logging</code> / <code>--no-request-logging</code>: request logging interceptor toggle</li> <li><code>--logger-name &lt;name&gt;</code>: logger name used by request logging interceptor</li> <li><code>--discover &lt;pattern&gt;</code>: autodiscovery pattern, default <code>{app}.grpc.services</code></li> </ul> <p>Examples:</p> <pre><code># development\npython manage.py run_grpcserver --bind 0.0.0.0:50051 --health --reflection --reload\n\n# production-like run\npython manage.py run_grpcserver \\\n  --bind 0.0.0.0:50051 \\\n  --max-workers 16 \\\n  --max-msg-mb 16 \\\n  --health \\\n  --reflection\n\n# custom auth backend\npython manage.py run_grpcserver --auth-backend project.grpc.auth.GrpcBearerAuth\n</code></pre>"},{"location":"commands/run-grpcserver/#autodiscovery","title":"Autodiscovery","text":"<p>By default command tries importing modules by pattern:</p> <p><code>{app}.grpc.services</code></p> <p>If your services live elsewhere, set custom pattern:</p> <pre><code>python manage.py run_grpcserver --discover {app}.api.grpc_services\n</code></pre>"},{"location":"commands/run-grpcserver/#reload-mode-notes","title":"Reload Mode Notes","text":"<p>With <code>--reload</code>, process manager respawns worker process on file change. During restart you may see <code>KeyboardInterrupt</code> traces in old worker process; this is expected shutdown behavior if server is being interrupted/restarted.</p> <p>Tune watched paths with settings and flags:</p> <pre><code>GRPC_EXTRA = {\n    \"reload_paths\": [\"app\"],\n}\n</code></pre> <p>And/or:</p> <pre><code>python manage.py run_grpcserver --reload --reload-path app --reload-path shared\n</code></pre>"},{"location":"commands/run-grpcserver/#health-and-reflection","title":"Health and Reflection","text":"<ul> <li><code>--health</code> registers standard gRPC health check service</li> <li><code>--reflection</code> exposes service descriptors for API tools</li> </ul> <p>If a service is not visible in client tools:</p> <ol> <li>ensure <code>--reflection</code> is enabled</li> <li>recreate connection in the client tool (some tools cache descriptors)</li> <li>verify service was discovered in startup logs</li> </ol>"},{"location":"commands/run-grpcserver/#authentication-integration","title":"Authentication Integration","text":"<p><code>--auth-backend</code> expects import path to backend implementation matching framework auth contract.</p> <p>Common startup failure:</p> <p><code>ModuleNotFoundError: No module named 'path'</code></p> <p>Reason: wrong import string in settings/flag (placeholder or typo). Use full Python import path to the backend symbol.</p>"},{"location":"commands/run-grpcserver/#common-errors","title":"Common Errors","text":""},{"location":"commands/run-grpcserver/#port-already-in-use","title":"Port already in use","text":"<p><code>Address already in use</code></p> <p>Fix: stop previous process or bind another port.</p>"},{"location":"commands/run-grpcserver/#unauthorized-for-all-methods","title":"Unauthorized for all methods","text":"<p>Check metadata key casing for gRPC transport (<code>authorization</code> is lowercase in metadata tools), auth scheme format, and backend parsing logic.</p>"},{"location":"commands/run-grpcserver/#reflection-enabled-but-service-missing","title":"Reflection enabled but service missing","text":"<p>Usually stale client-side descriptor cache. Reconnect/recreate request entry in tool.</p>"},{"location":"commands/run-grpcserver/#production-recommendations","title":"Production Recommendations","text":"<ol> <li>Run behind TLS-terminating edge or enable TLS in deployment topology.</li> <li>Keep reflection disabled in production unless explicitly needed.</li> <li>Keep health enabled for orchestrator probes.</li> <li>Configure worker count according to CPU and DB throughput profile.</li> <li>Keep request logging structured and avoid logging sensitive payloads.</li> </ol>"},{"location":"core/authentication/","title":"Authentication","text":"<p>This section describes how to plug authentication into gRPC server runtime.</p>"},{"location":"core/authentication/#auth-backend-contract","title":"Auth Backend Contract","text":"<p>Configure backend in settings:</p> <pre><code>GRPC_EXTRA = {\n    \"AUTH_BACKEND\": \"path.to.auth.GrpcBearerAuth\",\n}\n</code></pre> <p>Supported backend value types:</p> <ul> <li>callable</li> <li>class (instantiated without constructor arguments)</li> <li>import path string</li> </ul> <p>Runtime call signature:</p> <pre><code>def auth_backend(context, method: str, request=None):\n    ...\n</code></pre> <p>Return semantics:</p> <ul> <li><code>False</code> or <code>None</code> -&gt; request aborted with <code>UNAUTHENTICATED</code></li> <li>any truthy value -&gt; request allowed</li> </ul>"},{"location":"core/authentication/#grpcauthbase","title":"<code>GrpcAuthBase</code>","text":"<p>Minimal custom backend:</p> <pre><code>from grpc_extra.auth import GrpcAuthBase\n\n\nclass AllowHealthOnly(GrpcAuthBase):\n    PUBLIC = {\n        \"/grpc.health.v1.Health/Check\",\n        \"/grpc.health.v1.Health/Watch\",\n    }\n\n    def authenticate(self, context, method: str, request=None):\n        if method in self.PUBLIC:\n            return True\n        return False\n</code></pre>"},{"location":"core/authentication/#grpcbearerauthbase","title":"<code>GrpcBearerAuthBase</code>","text":"<p>Helper base for bearer-like token auth from metadata.</p> <p>Defaults:</p> <ul> <li><code>header = \"Authorization\"</code></li> <li><code>scheme = \"Bearer\"</code></li> </ul> <p>Matching is case-insensitive for gRPC metadata/header compatibility.</p>"},{"location":"core/authentication/#normal-bearer-mode","title":"Normal bearer mode","text":"<pre><code>from grpc_extra.auth import GrpcBearerAuthBase\n\n\nclass GrpcBearerAuth(GrpcBearerAuthBase):\n    def authenticate(self, context, token: str, method: str, request=None):\n        # validate token\n        user = get_user_from_token(token)\n        if user is None:\n            return None\n        setattr(context, \"user\", user)\n        return user\n</code></pre>"},{"location":"core/authentication/#raw-token-mode","title":"Raw token mode","text":"<p>If <code>scheme = \"\"</code>, full header value is treated as token.</p> <pre><code>class RawTokenAuth(GrpcBearerAuthBase):\n    scheme = \"\"\n\n    def authenticate(self, context, token: str, method: str, request=None):\n        user = validate_raw_token(token)\n        if not user:\n            return None\n        context.user = user\n        return user\n</code></pre>"},{"location":"core/authentication/#oidcjwt-example","title":"OIDC/JWT Example","text":"<pre><code>from grpc_extra.auth import GrpcBearerAuthBase\n\n\nclass OidcGrpcAuth(GrpcBearerAuthBase):\n    header = \"Authorization\"\n    scheme = \"Bearer\"\n\n    PUBLIC_METHODS = {\n        \"/grpc.health.v1.Health/Check\",\n        \"/grpc.health.v1.Health/Watch\",\n    }\n\n    def __call__(self, context, method: str, request=None):\n        if method in self.PUBLIC_METHODS:\n            return True\n        return super().__call__(context, method, request)\n\n    def authenticate(self, context, token: str, method: str, request=None):\n        client = OidcClient()\n        try:\n            claims = client.validate_access_token(token)\n            profile = client.resolve_profile(claims, token)\n            identity = client.resolve_identity(profile, claims)\n        except OidcAuthError:\n            return None\n\n        setattr(context, \"user\", identity)\n        return identity\n</code></pre>"},{"location":"core/authentication/#common-pitfalls","title":"Common pitfalls","text":""},{"location":"core/authentication/#1-method-never-reaches-authenticate","title":"1) Method never reaches <code>authenticate(...)</code>","text":"<p>Usually token extraction failed:</p> <ul> <li>missing header</li> <li>wrong scheme prefix</li> <li>metadata key mismatch in client tool</li> </ul> <p>Add debug log in <code>__call__</code> or in <code>_extract_token</code> path.</p>"},{"location":"core/authentication/#2-tls-certificate-errors-when-validating-tokenjwks","title":"2) TLS certificate errors when validating token/JWKS","text":"<p>If IdP uses internal/self-signed CA, configure CA for all HTTP/JWKS clients.</p>"},{"location":"core/authentication/#3-health-endpoint-requires-token","title":"3) Health endpoint requires token","text":"<p>Whitelist health methods explicitly (see examples above).</p>"},{"location":"core/authentication/#recommended-pattern","title":"Recommended pattern","text":"<ul> <li>Keep backend thin (extract + validate + attach identity).</li> <li>Put provider-specific HTTP/JWKS logic into dedicated client class.</li> <li>Attach resolved user/principal to <code>context.user</code> for permission checks.</li> </ul>"},{"location":"core/decorators/","title":"Core Decorators","text":"<p>This section explains how decorators define runtime behavior, proto generation, and request/response schemas.</p>"},{"location":"core/decorators/#grpc_service","title":"<code>@grpc_service</code>","text":"<p>Declares a class as a gRPC service and registers metadata.</p> <pre><code>from grpc_extra import grpc_service\n\n\n@grpc_service(\n    name=\"ExampleService\",\n    app_label=\"products\",\n    package=\"products\",\n    proto_path=\"grpc/proto/products.proto\",\n    permissions=[],\n)\nclass ExampleService:\n    pass\n</code></pre>"},{"location":"core/decorators/#parameters","title":"Parameters","text":"<ul> <li><code>name</code>: service name in proto (<code>service &lt;name&gt; { ... }</code>). Default: class name.</li> <li><code>app_label</code>: Django app label used for discovery and proto path resolution.</li> <li><code>package</code>: proto package name. Default: <code>app_label</code>.</li> <li><code>proto_path</code>: target proto path inside app. Default: <code>grpc/proto/&lt;app_label&gt;.proto</code>.</li> <li><code>description</code>: doc string written as proto comment.</li> <li><code>factory</code>: custom service instance factory for runtime.</li> <li><code>permissions</code>: service-level permission classes/instances/import paths.</li> </ul>"},{"location":"core/decorators/#grpc_method","title":"<code>@grpc_method</code>","text":"<p>Declares an RPC endpoint and schema contract.</p> <pre><code>from pydantic import BaseModel\nfrom grpc_extra import grpc_method\n\n\nclass PingRequest(BaseModel):\n    message: str\n\n\nclass PingResponse(BaseModel):\n    message: str\n\n\nclass ExampleService:\n    @grpc_method(request_schema=PingRequest, response_schema=PingResponse)\n    def ping(self, request, context):\n        return {\"message\": f\"pong: {request.message}\"}\n</code></pre>"},{"location":"core/decorators/#parameters_1","title":"Parameters","text":"<ul> <li><code>name</code>: RPC method name in proto. Default: function name converted to <code>UpperCamelCase</code>.</li> <li><code>request_schema</code>: Pydantic model for input.</li> <li><code>response_schema</code>: Pydantic model for output.</li> <li><code>description</code>: comment in proto.</li> <li><code>client_streaming</code>: request stream mode.</li> <li><code>server_streaming</code>: response stream mode.</li> <li><code>permissions</code>: method-level permissions.</li> </ul>"},{"location":"core/decorators/#top-level-list-response","title":"Top-level list response","text":"<p>Unary methods can return list payloads via:</p> <pre><code>@grpc_method(response_schema=list[ItemSchema])\ndef list_items(self, request, context):\n    return Item.objects.values(\"id\", \"name\")\n</code></pre> <p>The framework wraps this into an internal response schema with <code>items: repeated ItemSchema</code>.</p>"},{"location":"core/decorators/#grpc_pagination","title":"<code>@grpc_pagination</code>","text":"<p>Adds limit/offset request fields and paginated response schema.</p> <p>Supported forms:</p> <ul> <li><code>@grpc_pagination</code></li> <li><code>@grpc_pagination()</code></li> <li><code>@grpc_pagination(CustomPaginationClass)</code></li> </ul> <p>Example:</p> <pre><code>from grpc_extra import grpc_method, grpc_pagination\n\n\n@grpc_method(response_schema=ItemOut)\n@grpc_pagination\n# or @grpc_pagination()\ndef list_items(self, request, context):\n    return Item.objects.all()\n</code></pre>"},{"location":"core/decorators/#runtime-effect","title":"Runtime effect","text":"<ul> <li>Request schema gets <code>limit</code> and <code>offset</code>.</li> <li>Response schema is wrapped by pagination class (default: <code>count/limit/offset/results</code>).</li> </ul>"},{"location":"core/decorators/#important-constraints","title":"Important constraints","text":"<ul> <li>Must be placed under <code>@grpc_method</code>.</li> <li>Only for unary methods.</li> <li>Requires <code>response_schema</code>.</li> </ul>"},{"location":"core/decorators/#grpc_ordering","title":"<code>@grpc_ordering</code>","text":"<p>Adds ordering support via request field <code>ordering</code>.</p> <p>Supported forms:</p> <ul> <li><code>@grpc_ordering(ordering_fields=[...])</code></li> <li><code>@grpc_ordering(Ordering, ordering_fields=[...])</code></li> <li><code>@grpc_ordering(CustomOrdering, fields=[...])</code></li> </ul> <p>Example:</p> <pre><code>from grpc_extra import grpc_method, grpc_ordering\n\n\n@grpc_method(request_schema=ItemFilter, response_schema=ItemOut)\n@grpc_ordering(ordering_fields=[\"name\", \"sku\"])\ndef list_items(self, request, context):\n    return Item.objects.all()\n</code></pre>"},{"location":"core/decorators/#field-requirements","title":"Field requirements","text":"<p>By default, fields are required explicitly.</p> <p>If no fields are passed, decorator raises <code>ValueError</code>.</p> <p>For custom classes this is configurable via class contract:</p> <pre><code>from grpc_extra.ordering import BaseOrdering\n\n\nclass CustomOrdering(BaseOrdering):\n    fields_param_name = \"fields\"\n    fields_required = True\n\n    def __init__(self, fields):\n        self.fields = fields\n\n    def order(self, items, request):\n        return items\n</code></pre> <p>Now decorator can be used as:</p> <pre><code>@grpc_ordering(CustomOrdering, fields=[\"name\"])\n</code></pre>"},{"location":"core/decorators/#grpc_searching","title":"<code>@grpc_searching</code>","text":"<p>Adds search support via request field <code>search</code>.</p> <p>Supported forms:</p> <ul> <li><code>@grpc_searching(search_fields=[...])</code></li> <li><code>@grpc_searching(Searching, search_fields=[...])</code></li> <li><code>@grpc_searching(CustomSearching, fields=[...])</code></li> </ul> <p>Example:</p> <pre><code>from grpc_extra import grpc_method, grpc_searching\n\n\n@grpc_method(request_schema=ItemFilter, response_schema=ItemOut)\n@grpc_searching(search_fields=[\"name\", \"=sku\", \"description\"])\ndef list_items(self, request, context):\n    return Item.objects.values(\"id\", \"name\", \"sku\", \"description\")\n</code></pre> <p>Lookup prefixes:</p> <ul> <li><code>^field</code> -&gt; <code>istartswith</code></li> <li><code>=field</code> -&gt; <code>iexact</code></li> <li><code>@field</code> -&gt; <code>search</code></li> <li><code>$field</code> -&gt; <code>iregex</code></li> <li>default -&gt; <code>icontains</code></li> </ul>"},{"location":"core/decorators/#field-requirements_1","title":"Field requirements","text":"<p>Like ordering, fields are explicit by default.</p> <p>Custom class contract:</p> <pre><code>from grpc_extra.searching import BaseSearching\n\n\nclass CustomSearching(BaseSearching):\n    fields_param_name = \"fields\"\n    fields_required = True\n\n    def __init__(self, fields):\n        self.fields = fields\n\n    def search(self, items, request):\n        return items\n</code></pre>"},{"location":"core/decorators/#grpc_permissions","title":"<code>@grpc_permissions</code>","text":"<p>Adds method-level permissions.</p> <p>If method-level permissions are declared explicitly, they override service-level permissions for that RPC.</p> <pre><code>from grpc_extra import grpc_method, grpc_permissions, grpc_service\nfrom grpc_extra.permissions import AllowAny, IsAuthenticated\n\n\n@grpc_service(permissions=[IsAuthenticated])\nclass ExampleService:\n    @grpc_method(request_schema=PingRequest, response_schema=PingResponse)\n    @grpc_permissions(AllowAny)\n    def ping(self, request, context):\n        return {\"message\": \"ok\"}\n</code></pre> <p>Must be placed under <code>@grpc_method</code>.</p>"},{"location":"core/decorators/#decorator-order","title":"Decorator order","text":"<p>Recommended order (top -&gt; bottom):</p> <pre><code>@grpc_method(...)\n@grpc_pagination\n@grpc_ordering(ordering_fields=[...])\n@grpc_searching(search_fields=[...])\ndef list_items(...):\n    ...\n</code></pre> <p>Why:</p> <ul> <li><code>grpc_searching</code> and <code>grpc_ordering</code> extend request schema first</li> <li><code>grpc_pagination</code> extends final request/response shape</li> <li><code>grpc_method</code> collects resulting metadata</li> </ul>"},{"location":"core/decorators/#full-example","title":"Full example","text":"<pre><code>from pydantic import BaseModel\nfrom grpc_extra import (\n    grpc_service,\n    grpc_method,\n    grpc_pagination,\n    grpc_ordering,\n    grpc_searching,\n)\n\n\nclass ProductFilter(BaseModel):\n    is_active: bool | None = None\n\n\nclass ProductOut(BaseModel):\n    id: int\n    sku: str\n    name: str | None\n\n\n@grpc_service(name=\"ProductService\", app_label=\"products\", package=\"products\")\nclass ProductService:\n    @grpc_method(name=\"List\", request_schema=ProductFilter, response_schema=ProductOut)\n    @grpc_pagination\n    @grpc_ordering(ordering_fields=[\"sku\", \"name\"])\n    @grpc_searching(search_fields=[\"sku\", \"name\"])\n    def list(self, request, context):\n        qs = Product.objects.all().values(\"id\", \"sku\", \"name\")\n        if request.is_active is not None:\n            qs = qs.filter(is_active=request.is_active)\n        return qs\n</code></pre>"},{"location":"core/permissions/","title":"Permissions","text":""},{"location":"core/permissions/#base-contract","title":"Base Contract","text":"<pre><code>class BasePermission:\n    def has_perm(self, request, context, service, method_meta) -&gt; bool:\n        return True\n\n    def has_obj_perm(self, request, context, service, method_meta, obj) -&gt; bool:\n        return True\n</code></pre>"},{"location":"core/permissions/#where-to-declare","title":"Where to Declare","text":"<ul> <li>service-level: <code>@grpc_service(..., permissions=[...])</code></li> <li>method-level: <code>@grpc_method(..., permissions=[...])</code> or <code>@grpc_permissions(...)</code></li> </ul>"},{"location":"core/permissions/#runtime-behavior","title":"Runtime Behavior","text":"<ul> <li>service-level permissions act as the default policy for all methods</li> <li>if a method does not declare permissions, service-level <code>has_perm</code> is applied</li> <li>if a method declares permissions explicitly, method-level permissions override service-level permissions</li> <li>method-level permissions run both <code>has_perm</code> and <code>has_obj_perm</code></li> <li>for <code>Detail/Get</code>, service-level <code>has_obj_perm</code> is also applied only when the method does not override permissions</li> </ul> <p>This means you can keep a strict default policy on the service and open one RPC explicitly:</p> <pre><code>from pydantic import BaseModel\n\nfrom grpc_extra import grpc_method, grpc_permissions, grpc_service\nfrom grpc_extra.permissions import AllowAny, IsAuthActive\n\n\nclass StatusSchema(BaseModel):\n    value: int\n    label: str\n\n\n@grpc_service(\n    name=\"ProductService\",\n    app_label=\"products\",\n    package=\"products\",\n    permissions=[IsAuthActive],\n)\nclass ProductService:\n    @grpc_method(name=\"Statuses\", response_schema=list[StatusSchema])\n    @grpc_permissions(AllowAny)\n    def statuses(self, request, context):\n        return [\n            {\"value\": 1, \"label\": \"Active\"},\n            {\"value\": 2, \"label\": \"Archived\"},\n        ]\n</code></pre> <p>You can also explicitly remove inherited service permissions for a method:</p> <pre><code>@grpc_method(..., permissions=[])\ndef health_like_rpc(self, request, context):\n    ...\n</code></pre> <p>Built-ins:</p> <ul> <li><code>AllowAny</code></li> <li><code>IsAuthenticated</code></li> <li><code>IsAuthenticatedActive</code> (<code>IsAuthActive</code> alias)</li> <li><code>DjangoModelPermissions</code></li> </ul>"},{"location":"core/runtime/","title":"Runtime","text":"<p>This section describes what happens between incoming gRPC request and your service method result.</p>"},{"location":"core/runtime/#high-level-flow","title":"High-level Flow","text":"<p>For each RPC call runtime performs:</p> <ol> <li>decode incoming pb2 request into Pydantic schema</li> <li>apply service/method permissions</li> <li>call your Python method</li> <li>apply searching/ordering/pagination (if configured)</li> <li>encode method result into outgoing pb2 response</li> <li>map exceptions to gRPC status</li> </ol>"},{"location":"core/runtime/#request-decode","title":"Request Decode","text":"<p>Request decode function:</p> <ul> <li>input: protobuf message instance</li> <li>output: <code>request_schema</code> Pydantic model (or raw object if schema is <code>None</code>)</li> </ul>"},{"location":"core/runtime/#unary-request","title":"Unary request","text":"<pre><code>@grpc_method(request_schema=PingRequest, response_schema=PingResponse)\ndef ping(self, request, context):\n    # request is PingRequest here\n    return {\"message\": request.message}\n</code></pre>"},{"location":"core/runtime/#stream-request","title":"Stream request","text":"<p>For <code>client_streaming=True</code>, runtime decodes each incoming item lazily.</p>"},{"location":"core/runtime/#response-encode","title":"Response Encode","text":"<p>Encoder accepts these return types:</p> <ul> <li><code>dict</code></li> <li><code>pydantic.BaseModel</code></li> <li>dataclass instance</li> <li>Django model instance</li> <li>generic object with attributes</li> </ul> <p>Then it validates against <code>response_schema</code> and constructs pb2 response.</p>"},{"location":"core/runtime/#supported-method-shapes","title":"Supported Method Shapes","text":"<p>All four gRPC shapes are supported:</p> <ul> <li>unary-unary</li> <li>unary-stream</li> <li>stream-unary</li> <li>stream-stream</li> </ul>"},{"location":"core/runtime/#streaming-rules","title":"Streaming Rules","text":"<ul> <li>For server-streaming and bidi methods your method must return iterable.</li> <li>For stream-unary pagination is not supported.</li> <li>For stream responses object permissions are checked per yielded item.</li> </ul>"},{"location":"core/runtime/#searchorderpagination-pipeline","title":"Search/Order/Pagination Pipeline","text":"<p>For unary list-like responses runtime applies modifiers in this order:</p> <ol> <li>searching</li> <li>ordering</li> <li>pagination</li> </ol> <p>This order is used in ModelService list endpoints and decorator-based methods.</p>"},{"location":"core/runtime/#collection-and-wrapper-behavior","title":"Collection and Wrapper Behavior","text":""},{"location":"core/runtime/#top-level-list-response","title":"Top-level list response","text":"<p>If you declared:</p> <pre><code>@grpc_method(response_schema=list[ItemSchema])\n</code></pre> <p>runtime auto-wraps output into internal schema:</p> <pre><code>{\"items\": [...]} \n</code></pre> <p>Works with:</p> <ul> <li><code>list</code></li> <li><code>QuerySet</code></li> <li>iterable objects</li> <li>objects exposing <code>.iterator()</code></li> </ul>"},{"location":"core/runtime/#decimal-handling","title":"Decimal handling","text":"<p><code>Decimal</code> values are coerced to <code>string</code> before pb2 construction.</p> <p>Useful when Django <code>DecimalField</code> maps to proto <code>string</code>.</p>"},{"location":"core/runtime/#permissions-in-runtime","title":"Permissions in Runtime","text":"<p>Checks are executed before/after method call depending on type:</p> <ul> <li>service-level <code>has_perm</code> for all methods</li> <li>method-level <code>has_perm</code> for all methods</li> <li>method-level <code>has_obj_perm</code> for object/response checks</li> <li>service-level <code>has_obj_perm</code> also applies to <code>Detail/Get</code></li> </ul>"},{"location":"core/runtime/#exception-mapping","title":"Exception Mapping","text":"<p>Default mapping:</p> <ul> <li><code>pydantic.ValidationError</code> -&gt; <code>INVALID_ARGUMENT</code></li> <li>request decode with validation cause -&gt; <code>INVALID_ARGUMENT</code></li> <li><code>ObjectDoesNotExist</code> -&gt; <code>NOT_FOUND</code></li> <li><code>PermissionError</code> -&gt; <code>PERMISSION_DENIED</code></li> <li><code>OrderingError</code> / <code>SearchingError</code> -&gt; <code>INVALID_ARGUMENT</code></li> <li><code>RequestDecodeError</code> / <code>ResponseEncodeError</code> -&gt; <code>INTERNAL</code></li> <li>any other exception -&gt; <code>UNKNOWN</code></li> </ul>"},{"location":"core/runtime/#custom-exception-mapper","title":"Custom Exception Mapper","text":"<p>You can override mapper in settings:</p> <pre><code>GRPC_EXTRA = {\n    \"EXCEPTION_MAPPER\": \"path.to.custom_exception_mapper\",\n}\n</code></pre> <p>Mapper contract:</p> <pre><code>from grpc_extra.exceptions import MappedError\n\n\ndef custom_exception_mapper(exc: Exception) -&gt; MappedError:\n    ...\n</code></pre> <p><code>EXCEPTION_MAPPER</code> must resolve to callable (function/callable object), not class type.</p>"},{"location":"core/runtime/#runtime-pitfalls","title":"Runtime Pitfalls","text":""},{"location":"core/runtime/#1-invalid-wire-type","title":"1) <code>invalid wire type</code>","text":"<p>Usually client descriptor is stale after proto changes.</p> <p>Fix:</p> <ul> <li>regenerate proto/pb2</li> <li>restart server</li> <li>recreate gRPC connection in client tool</li> </ul>"},{"location":"core/runtime/#2-failed-to-encode-response","title":"2) <code>Failed to encode response</code>","text":"<p>Most often schema/payload mismatch.</p> <p>Typical causes:</p> <ul> <li>wrong field type in returned dict/model</li> <li>relation object returned where scalar is expected</li> <li>stale proto versus current schema</li> </ul>"},{"location":"core/runtime/#3-searchorder-silently-not-applied","title":"3) Search/order silently not applied","text":"<p>Ensure fields are configured explicitly in decorators/model config.</p>"},{"location":"core/runtime/#4-auth-backend-rejects-before-token-validate","title":"4) Auth backend rejects before token validate","text":"<p>Most often metadata extraction mismatch (header/scheme/value format).</p>"},{"location":"core/runtime/#performance-notes","title":"Performance Notes","text":"<ul> <li>Prefer returning <code>QuerySet</code> for large data, let runtime iterate lazily where possible.</li> <li>For model-heavy endpoints define <code>queryset</code> / <code>detail_queryset</code> with <code>select_related</code>/<code>prefetch_related</code>.</li> <li>Keep response schemas tight; avoid expensive nested relations unless needed.</li> </ul>"},{"location":"core/server/","title":"Server","text":"<p>This section explains how to run and operate gRPC server in Django using <code>django-grpc-extra</code>.</p>"},{"location":"core/server/#entry-points","title":"Entry Points","text":"<p>You can run server in two ways:</p> <ol> <li>management command (<code>run_grpcserver</code>)</li> <li>Python API (<code>GrpcExtra.run_server(...)</code>)</li> </ol>"},{"location":"core/server/#option-a-management-command","title":"Option A: Management Command","text":"<p>Basic run:</p> <pre><code>python manage.py run_grpcserver\n</code></pre> <p>Typical local run:</p> <pre><code>python manage.py run_grpcserver --bind 0.0.0.0:50051 --health --reflection\n</code></pre>"},{"location":"core/server/#important-flags","title":"Important flags","text":"<ul> <li><code>--bind</code>: server bind address (default from settings)</li> <li><code>--max-workers</code>: gRPC thread pool size</li> <li><code>--max-msg-mb</code>: send/receive message size limit</li> <li><code>--health</code> / <code>--no-health</code>: enable or disable health service</li> <li><code>--reflection</code> / <code>--no-reflection</code>: enable or disable server reflection</li> <li><code>--reload</code> / <code>--no-reload</code>: live reload mode</li> <li><code>--reload-path &lt;path&gt;</code> (repeatable): watched paths in reload mode</li> <li><code>--auth-backend</code>: import path for auth backend</li> <li><code>--interceptor</code> (repeatable): importable runtime interceptor</li> <li><code>--request-logging</code> / <code>--no-request-logging</code></li> <li><code>--logger-name</code>: logger used by request logging interceptor</li> <li><code>--discover</code> / <code>--no-discover</code>: service autodiscovery toggle</li> </ul>"},{"location":"core/server/#option-b-python-api","title":"Option B: Python API","text":"<pre><code>import os\nimport django\nfrom grpc_extra import GrpcExtra\n\n\ndef main() -&gt; None:\n    os.environ.setdefault(\"DJANGO_SETTINGS_MODULE\", \"example_project.settings\")\n    django.setup()\n\n    grpc_extra = GrpcExtra(pattern=\"{app}.grpc.services\")\n    grpc_extra.run_server(\n        bind=\"0.0.0.0:50051\",\n        enable_health=True,\n        enable_reflection=True,\n    )\n\n\nif __name__ == \"__main__\":\n    main()\n</code></pre>"},{"location":"core/server/#service-discovery","title":"Service Discovery","text":"<p>By default, server autodiscovers modules by pattern:</p> <pre><code>\"{app}.grpc.services\"\n</code></pre> <p>Meaning each Django app should expose decorated services in <code>grpc/services.py</code>.</p> <p>You can override pattern in <code>GrpcExtra(pattern=...)</code>.</p>"},{"location":"core/server/#runtime-components","title":"Runtime Components","text":"<p>When server starts:</p> <ol> <li>autodiscovers decorated services</li> <li>imports generated <code>*_pb2.py</code> and <code>*_pb2_grpc.py</code></li> <li>adapts methods through runtime adapter</li> <li>registers servicers in gRPC server</li> <li>optionally adds health/reflection</li> <li>starts listening on bind address</li> </ol>"},{"location":"core/server/#health-and-reflection","title":"Health and Reflection","text":""},{"location":"core/server/#health","title":"Health","text":"<p>Enable for service health checks:</p> <pre><code>python manage.py run_grpcserver --health\n</code></pre>"},{"location":"core/server/#reflection","title":"Reflection","text":"<p>Enable for tools like Postman/grpcurl schema introspection:</p> <pre><code>python manage.py run_grpcserver --reflection\n</code></pre> <p>If reflection is enabled, remember to recreate connection in some clients after schema changes.</p>"},{"location":"core/server/#interceptors","title":"Interceptors","text":"<p>Runtime supports:</p> <ul> <li>auth interceptor (when auth backend configured)</li> <li>request logging interceptor (when enabled)</li> <li>custom interceptors from settings/CLI</li> </ul>"},{"location":"core/server/#custom-interceptor","title":"Custom interceptor","text":"<pre><code>GRPC_EXTRA = {\n    \"INTERCEPTORS\": [\n        \"path.to.CustomServerInterceptor\",\n    ]\n}\n</code></pre> <p>For reload mode, prefer importable interceptors via settings (not runtime object instances).</p>"},{"location":"core/server/#live-reload","title":"Live Reload","text":"<p>Enable reload in dev:</p> <pre><code>python manage.py run_grpcserver --reload --reload-path app\n</code></pre> <p>Or in settings:</p> <pre><code>GRPC_EXTRA = {\n    \"ENABLE_RELOAD\": True,\n    \"RELOAD_PATHS\": (\"app\",),\n}\n</code></pre>"},{"location":"core/server/#notes","title":"Notes","text":"<ul> <li>reload mode uses <code>watchfiles</code></li> <li>if package is missing, install <code>django-grpc-extra[reload]</code></li> <li>on worker restart <code>KeyboardInterrupt</code> may appear internally and is handled by runtime</li> </ul>"},{"location":"core/server/#auth-integration","title":"Auth Integration","text":"<p>Configure backend (callable/class/import path):</p> <pre><code>GRPC_EXTRA = {\n    \"AUTH_BACKEND\": \"path.to.auth.GrpcBearerAuth\",\n}\n</code></pre> <p>See Authentication for full contract.</p>"},{"location":"core/server/#message-size-limits","title":"Message Size Limits","text":"<p>Set max message size (MB):</p> <pre><code>GRPC_EXTRA = {\n    \"MAX_MSG_MB\": 64,\n}\n</code></pre> <p>This is applied to both send and receive options.</p>"},{"location":"core/server/#logging","title":"Logging","text":"<p>Request logs are controlled by:</p> <pre><code>GRPC_EXTRA = {\n    \"ENABLE_REQUEST_LOGGING\": True,\n    \"LOGGER_NAME\": \"grpc_extra\",\n}\n</code></pre>"},{"location":"core/server/#production-notes","title":"Production Notes","text":"<ul> <li>Keep reflection disabled in production unless needed.</li> <li>Prefer explicit auth and permissions for all non-public RPCs.</li> <li>Set conservative message limits.</li> <li>Use dedicated process management (systemd/docker/k8s) and health checks.</li> <li>Keep proto generation and app deployment in sync.</li> </ul>"},{"location":"core/server/#common-server-errors","title":"Common Server Errors","text":""},{"location":"core/server/#pb2_grpc-module-not-found","title":"<code>pb2_grpc module not found</code>","text":"<p>Cause: proto files were not generated.</p> <p>Fix:</p> <pre><code>python manage.py generate_proto --all\n</code></pre>"},{"location":"core/server/#modulenotfounderror-for-app-grpc-module","title":"<code>ModuleNotFoundError</code> for app grpc module","text":"<p>Cause: discovery pattern mismatch or missing <code>grpc/services.py</code> module.</p>"},{"location":"core/server/#reflection-doesnt-show-service","title":"Reflection doesn't show service","text":"<p>Cause: stale client connection cache.</p> <p>Fix: recreate connection after server/proto restart.</p>"},{"location":"core/testing/","title":"Testing","text":"<p><code>grpc_extra</code> includes an in-process testing helper for service integration tests.</p> <p>The testing client is intentionally small:</p> <ul> <li>it supports <code>unary-unary</code> RPC methods;</li> <li>it runs in-process without starting a real gRPC server;</li> <li>it uses the existing runtime adapter, so request decoding, permissions,   pagination, searching, ordering, exception mapping, and response encoding are   exercised in tests.</li> </ul>"},{"location":"core/testing/#public-api","title":"Public API","text":"<p>Available helpers:</p> <ul> <li><code>GrpcTestClient</code></li> <li><code>GrpcTestResponse</code></li> <li><code>TestServicerContext</code></li> <li><code>make_pb2_module</code></li> </ul> <p>Import them directly from <code>grpc_extra</code>:</p> <pre><code>from grpc_extra import GrpcTestClient, TestServicerContext, make_pb2_module\n</code></pre>"},{"location":"core/testing/#fake-pb2-module","title":"Fake pb2 Module","text":"<p><code>make_pb2_module</code> builds a minimal in-memory pb2 stub so tests do not require pre-generated protobuf artifacts.</p> <pre><code>pb2 = make_pb2_module(\n    \"UserService\",\n    {\n        \"List\": \"ListUsersResponse\",\n        \"Retrieve\": \"UserResponse\",\n    },\n)\n</code></pre> <p>The first argument is the gRPC service name as it appears in the <code>.proto</code> file. The second argument maps each gRPC method name to its response message name.</p> <p>Pass the result as <code>pb2_module</code> when calling <code>GrpcTestClient.call</code>:</p> <pre><code>response = GrpcTestClient().call(UserService, \"List\", {\"limit\": 10}, pb2_module=pb2)\n</code></pre> <p>This is the recommended approach for integration tests that hit a real database \u2014 it keeps your test suite independent of the proto compilation step.</p>"},{"location":"core/testing/#basic-usage","title":"Basic Usage","text":"<pre><code>from pydantic import BaseModel\n\nfrom grpc_extra import GrpcTestClient, grpc_method, grpc_service\n\n\nclass EchoRequest(BaseModel):\n    value: int\n\n\nclass EchoResponse(BaseModel):\n    value: int\n\n\n@grpc_service(app_label=\"products\", package=\"products\")\nclass EchoService:\n    @grpc_method(request_schema=EchoRequest, response_schema=EchoResponse)\n    def echo(self, request, context):\n        return {\"value\": request.value}\n\n\nclient = GrpcTestClient()\nresponse = client.call(EchoService, \"Echo\", {\"value\": 7}, pb2_module=pb2_module)\n\nassert response.ok is True\nassert response.data == {\"value\": 7}\nassert response.code.name == \"OK\"\n</code></pre>"},{"location":"core/testing/#what-call-accepts","title":"What <code>call(...)</code> Accepts","text":"<pre><code>response = client.call(\n    service=EchoService,\n    method=\"Echo\",\n    request={\"value\": 7},\n    metadata=[(\"authorization\", \"Bearer token\")],\n    context=None,\n    pb2_module=pb2_module,\n    auth_backend=None,\n)\n</code></pre> <p>Arguments:</p> <ul> <li><code>service</code></li> <li>decorated service class</li> <li>service instance</li> <li><code>ServiceDefinition</code></li> <li><code>method</code></li> <li>gRPC method name (<code>\"Echo\"</code>)</li> <li>or Python handler name (<code>\"echo\"</code>)</li> <li><code>request</code></li> <li><code>dict</code></li> <li>Pydantic model</li> <li>protobuf request object</li> <li><code>metadata</code></li> <li>request metadata for <code>context.invocation_metadata()</code></li> <li><code>context</code></li> <li>custom <code>TestServicerContext</code></li> <li><code>pb2_module</code></li> <li>optional explicit pb2 module override</li> <li><code>auth_backend</code></li> <li>optional backend for this call only</li> </ul>"},{"location":"core/testing/#response-object","title":"Response Object","text":"<p><code>GrpcTestResponse</code> gives you both transport information and decoded payload.</p> <pre><code>response = client.call(...)\n\nresponse.ok\nresponse.code\nresponse.details\nresponse.message\nresponse.data\nresponse.initial_metadata\nresponse.trailing_metadata\nresponse.json()\nresponse.assert_ok()\n</code></pre> <p>Notes:</p> <ul> <li><code>response.message</code> is the raw protobuf response object.</li> <li><code>response.data</code> and <code>response.json()</code> return a plain Python representation.</li> </ul>"},{"location":"core/testing/#metadata-and-custom-context","title":"Metadata And Custom Context","text":"<p>Use request metadata:</p> <pre><code>response = client.call(\n    EchoService,\n    \"Echo\",\n    {\"value\": 1},\n    metadata=[(\"x-trace-id\", \"123\")],\n    pb2_module=pb2_module,\n)\n</code></pre> <p>Use a prebuilt context when you want to attach custom state:</p> <pre><code>context = TestServicerContext(user=my_user, tenant_id=\"acme\")\n\nresponse = client.call(\n    EchoService,\n    \"Echo\",\n    {\"value\": 1},\n    context=context,\n    pb2_module=pb2_module,\n)\n</code></pre>"},{"location":"core/testing/#testing-auth","title":"Testing Auth","text":"<p><code>GrpcTestClient</code> can run an auth backend before the method call.</p> <pre><code>def auth_backend(context, method, request):\n    context.user = user\n    return user\n\n\nclient = GrpcTestClient(auth_backend=auth_backend)\nresponse = client.call(SecureService, \"List\", {\"limit\": 10}, pb2_module=pb2_module)\n</code></pre> <p>If the backend returns <code>None</code> or <code>False</code>, the client returns:</p> <ul> <li><code>code == grpc.StatusCode.UNAUTHENTICATED</code></li> <li><code>details == \"Unauthorized\"</code></li> </ul>"},{"location":"core/testing/#testing-modelservice","title":"Testing ModelService","text":"<p>This helper is especially useful for <code>ModelService</code>, because you can exercise:</p> <ul> <li>request validation;</li> <li>generated CRUD endpoints;</li> <li>searching;</li> <li>ordering;</li> <li>pagination;</li> <li>permissions;</li> <li>generated choice endpoints.</li> </ul> <pre><code>response = client.call(\n    ProductService,\n    \"List\",\n    {\"limit\": 10, \"offset\": 0},\n    pb2_module=pb2_module,\n)\n\nassert response.ok\nassert response.data[\"count\"] &gt;= 0\nassert \"results\" in response.data\n</code></pre>"},{"location":"core/testing/#limitations","title":"Limitations","text":"<p>Current MVP limitations:</p> <ul> <li>only <code>unary-unary</code> methods are supported;</li> <li>it does not start a real gRPC server;</li> <li>interceptors are not executed automatically;</li> <li>reflection/health endpoints are outside this helper's scope.</li> </ul> <p>For <code>stream-unary</code>, <code>unary-stream</code>, or <code>stream-stream</code>, use direct adapter tests or a real server/channel setup.</p>"},{"location":"migration/notes/","title":"Migration Notes","text":""},{"location":"migration/notes/#beta-notes","title":"Beta Notes","text":"<ul> <li>Decorator contracts are stabilizing.</li> <li>Prefer explicit <code>search_fields</code> and <code>ordering_fields</code>.</li> <li>Re-generate proto after changing schemas/decorators.</li> </ul>"},{"location":"model-service/filtering/","title":"Model Service Filtering","text":"<p>Filtering in <code>ModelService</code> is configured through <code>ModelServiceConfig.list_filter</code>.</p> <p><code>list_filter</code> affects request schema for:</p> <ul> <li><code>List</code></li> <li><code>StreamList</code></li> </ul>"},{"location":"model-service/filtering/#two-filtering-modes","title":"Two Filtering Modes","text":"<ol> <li>plain Pydantic schema (exact field matching)</li> <li><code>ModelFilterSchema</code> (operator-based filtering via field metadata)</li> </ol>"},{"location":"model-service/filtering/#1-plain-pydantic-filter-schema","title":"1) Plain Pydantic filter schema","text":"<p>Use this when exact equality is enough.</p> <pre><code>from pydantic import BaseModel\n\n\nclass ProductListFilter(BaseModel):\n    category_id: int | None = None\n    is_active: bool | None = None\n</code></pre> <p>Attach in config:</p> <pre><code>config = ModelServiceConfig(\n    model=Product,\n    allowed_endpoints=[AllowedEndpoints.LIST],\n    list_schema=ProductOut,\n    list_filter=ProductListFilter,\n)\n</code></pre> <p>Request example:</p> <pre><code>{\n  \"category_id\": 10,\n  \"is_active\": true\n}\n</code></pre> <p>Equivalent queryset intent:</p> <pre><code>Product.objects.filter(category_id=10, is_active=True)\n</code></pre>"},{"location":"model-service/filtering/#2-modelfilterschema-with-operators","title":"2) <code>ModelFilterSchema</code> with operators","text":"<p>Use <code>ModelFilterSchema</code> when you need richer filtering (<code>in</code>, ranges, etc.).</p> <pre><code>from pydantic import Field\nfrom grpc_extra import ModelFilterSchema\n\n\nclass ProductListFilter(ModelFilterSchema):\n    category_id: int | None = None\n\n    ids: list[int] | None = Field(\n        default=None,\n        json_schema_extra={\"op\": \"in\", \"field\": \"id\"},\n        description=\"Filter by product IDs\",\n    )\n\n    min_price: float | None = Field(\n        default=None,\n        json_schema_extra={\"op\": \"gte\", \"field\": \"price\"},\n    )\n\n    max_price: float | None = Field(\n        default=None,\n        json_schema_extra={\"op\": \"lte\", \"field\": \"price\"},\n    )\n</code></pre> <p>Request example:</p> <pre><code>{\n  \"ids\": [101, 102, 103],\n  \"min_price\": 10,\n  \"max_price\": 100\n}\n</code></pre> <p>Equivalent queryset intent:</p> <pre><code>Product.objects.filter(\n    id__in=[101, 102, 103],\n    price__gte=10,\n    price__lte=100,\n)\n</code></pre>"},{"location":"model-service/filtering/#supported-operators","title":"Supported operators","text":"<p>Built-in operator set:</p> <ul> <li><code>exact</code> (default)</li> <li><code>in</code></li> <li><code>not_in</code></li> <li><code>lt</code></li> <li><code>gt</code></li> <li><code>lte</code></li> <li><code>gte</code></li> </ul> <p>If <code>op</code> is omitted, <code>exact</code> is used.</p>"},{"location":"model-service/filtering/#operator-mapping-examples","title":"Operator mapping examples","text":""},{"location":"model-service/filtering/#exact-default","title":"<code>exact</code> (default)","text":"<pre><code>status: int | None = Field(default=None, json_schema_extra={\"field\": \"status\"})\n</code></pre> <p>Request:</p> <pre><code>{\"status\": 2}\n</code></pre> <p>Intent:</p> <pre><code>qs.filter(status=2)\n</code></pre>"},{"location":"model-service/filtering/#in","title":"<code>in</code>","text":"<pre><code>statuses: list[int] | None = Field(\n    default=None,\n    json_schema_extra={\"field\": \"status\", \"op\": \"in\"},\n)\n</code></pre> <p>Request:</p> <pre><code>{\"statuses\": [1, 2, 3]}\n</code></pre> <p>Intent:</p> <pre><code>qs.filter(status__in=[1, 2, 3])\n</code></pre>"},{"location":"model-service/filtering/#not_in","title":"<code>not_in</code>","text":"<pre><code>excluded_ids: list[int] | None = Field(\n    default=None,\n    json_schema_extra={\"field\": \"id\", \"op\": \"not_in\"},\n)\n</code></pre> <p>Intent:</p> <pre><code>qs.exclude(id__in=[...])\n</code></pre>"},{"location":"model-service/filtering/#range-gtgteltlte","title":"range (<code>gt</code>/<code>gte</code>/<code>lt</code>/<code>lte</code>)","text":"<pre><code>created_from: str | None = Field(\n    default=None,\n    json_schema_extra={\"field\": \"created_at\", \"op\": \"gte\"},\n)\ncreated_to: str | None = Field(\n    default=None,\n    json_schema_extra={\"field\": \"created_at\", \"op\": \"lte\"},\n)\n</code></pre> <p>Intent:</p> <pre><code>qs.filter(created_at__gte=..., created_at__lte=...)\n</code></pre>"},{"location":"model-service/filtering/#full-modelservice-example","title":"Full <code>ModelService</code> example","text":"<pre><code>from pydantic import BaseModel, ConfigDict, Field\nfrom grpc_extra import AllowedEndpoints, ModelFilterSchema, ModelService, ModelServiceConfig, grpc_service\n\n\nclass ItemOut(BaseModel):\n    model_config = ConfigDict(from_attributes=True)\n    id: int\n    sku: str\n    name: str | None\n\n\nclass ItemFilter(ModelFilterSchema):\n    is_active: bool | None = None\n\n    ids: list[int] | None = Field(\n        default=None,\n        json_schema_extra={\"op\": \"in\", \"field\": \"id\"},\n    )\n\n    min_id: int | None = Field(\n        default=None,\n        json_schema_extra={\"op\": \"gte\", \"field\": \"id\"},\n    )\n\n\n@grpc_service(app_label=\"products\", package=\"products\")\nclass ItemService(ModelService):\n    config = ModelServiceConfig(\n        model=Item,\n        allowed_endpoints=[AllowedEndpoints.LIST, AllowedEndpoints.STREAM_LIST],\n        list_schema=ItemOut,\n        list_filter=ItemFilter,\n    )\n</code></pre>"},{"location":"model-service/filtering/#filtering-searchingorderingpagination","title":"Filtering + searching/ordering/pagination","text":"<p>List runtime order remains:</p> <ol> <li>filtering (from <code>list_filter</code>)</li> <li>searching</li> <li>ordering</li> <li>pagination</li> </ol> <p>So <code>list_filter</code> narrows base dataset first.</p>"},{"location":"model-service/filtering/#validation-and-error-behavior","title":"Validation and error behavior","text":"<p>Typical invalid requests return <code>INVALID_ARGUMENT</code>:</p> <ul> <li>wrong field type (e.g. string where <code>int</code> expected)</li> <li>invalid list element type for <code>in</code></li> <li>unknown/invalid operator in custom metadata</li> </ul>"},{"location":"model-service/filtering/#best-practices","title":"Best practices","text":"<ol> <li>Keep filter schema explicit and small.</li> <li>Use descriptive field names (<code>min_price</code>, <code>created_from</code>) and map to model fields with <code>json_schema_extra</code>.</li> <li>Prefer <code>ModelFilterSchema</code> for range and list operations.</li> <li>Add titles/descriptions to fields so generated proto comments are informative.</li> <li>Keep <code>list_filter</code> stable to avoid client request-contract churn.</li> </ol>"},{"location":"model-service/overview/","title":"Model Service Overview","text":"<p><code>ModelService</code> is for CRUD-style endpoints over Django models with minimal boilerplate.</p> <p>It auto-generates RPC methods from <code>ModelServiceConfig</code> and integrates filtering, searching, ordering and pagination.</p>"},{"location":"model-service/overview/#generated-endpoints","title":"Generated Endpoints","text":"<p>Depending on <code>allowed_endpoints</code>:</p> <ul> <li><code>List</code> (unary list)</li> <li><code>StreamList</code> (server stream)</li> <li><code>Detail</code></li> <li><code>Create</code></li> <li><code>Update</code></li> <li><code>Patch</code></li> <li><code>Delete</code></li> </ul>"},{"location":"model-service/overview/#minimal-example","title":"Minimal Example","text":"<pre><code>from pydantic import BaseModel, ConfigDict\nfrom grpc_extra import (\n    AllowedEndpoints,\n    ModelService,\n    ModelServiceConfig,\n    grpc_service,\n)\n\n\nclass ProductOut(BaseModel):\n    model_config = ConfigDict(from_attributes=True)\n    id: int\n    name: str\n\n\n@grpc_service(app_label=\"products\", package=\"products\")\nclass ProductService(ModelService):\n    config = ModelServiceConfig(\n        model=Product,\n        allowed_endpoints=[\n            AllowedEndpoints.LIST,\n            AllowedEndpoints.DETAIL,\n            AllowedEndpoints.CREATE,\n        ],\n        list_schema=ProductOut,\n        detail_schema=ProductOut,\n        create_schema=ProductOut,\n    )\n</code></pre>"},{"location":"model-service/overview/#extended-example-with-list-customizations","title":"Extended Example (with list customizations)","text":"<pre><code>from grpc_extra import (\n    AllowedEndpoints as Endpoints,\n    LimitOffsetPagination,\n    ModelService,\n    ModelServiceConfig,\n    Ordering,\n    Searching,\n    grpc_service,\n)\n\n\n@grpc_service(name=\"ProductService\", app_label=\"products\", package=\"products\")\nclass ProductService(ModelService):\n    config = ModelServiceConfig(\n        model=Product,\n        allowed_endpoints=[Endpoints.LIST, Endpoints.DETAIL],\n        list_schema=ProductListSchema,\n        detail_schema=ProductDetailSchema,\n        list_pagination_class=LimitOffsetPagination,\n        list_ordering_class=Ordering,\n        list_ordering_fields=[\"sku\", \"name\"],\n        list_searching_class=Searching,\n        list_search_fields=[\"sku\", \"name\", \"description\"],\n    )\n</code></pre> <p>Runtime order for list endpoint:</p> <ol> <li>searching</li> <li>ordering</li> <li>pagination</li> </ol>"},{"location":"model-service/overview/#queryset-customization","title":"QuerySet Customization","text":"<p>For performance and relation preloading:</p> <ul> <li><code>queryset</code>: base queryset for list and general operations</li> <li><code>detail_queryset</code>: dedicated queryset for detail endpoint</li> </ul> <pre><code>config = ModelServiceConfig(\n    model=Product,\n    queryset=Product.objects.select_related(\"category\", \"brand\"),\n    detail_queryset=Product.objects.select_related(\n        \"category\",\n        \"brand\",\n        \"supplier\",\n        \"warehouse\",\n    ),\n    ...\n)\n</code></pre> <p>Why this matters:</p> <ul> <li>list/detail often need different relation graphs</li> <li>avoids N+1 queries</li> <li>keeps endpoint-specific performance predictable</li> </ul>"},{"location":"model-service/overview/#generated-choice-endpoints","title":"Generated Choice Endpoints","text":"<p><code>ModelService</code> can also generate read-only RPCs for Django <code>IntegerChoices</code> and <code>TextChoices</code>.</p> <p>This is useful when the service should expose enum-like reference data for clients, DWH services, forms, or other integrations without hand-writing one method per choice set.</p> <pre><code>from django.db import models\n\nfrom grpc_extra import (\n    AllowedEndpoints as Endpoints,\n    ChoiceEndpointConfig,\n    ModelService,\n    ModelServiceConfig,\n    grpc_service,\n)\n\n\nclass ProductStatus(models.IntegerChoices):\n    ACTIVE = 1, \"Active\"\n    ARCHIVED = 2, \"Archived\"\n\n\n@grpc_service(\n    name=\"ProductService\",\n    app_label=\"products\",\n    package=\"products\",\n    permissions=[IsAuthActive],\n)\nclass ProductService(ModelService):\n    config = ModelServiceConfig(\n        model=Product,\n        allowed_endpoints=[Endpoints.LIST, Endpoints.DETAIL],\n        list_schema=ProductListSchema,\n        detail_schema=ProductDetailSchema,\n        choice_endpoints=[\n            ChoiceEndpointConfig(\n                name=\"Statuses\",\n                source=ProductStatus,\n                description=\"List available product statuses.\",\n            ),\n        ],\n    )\n</code></pre> <p>The builder generates:</p> <ul> <li>python handler: <code>statuses</code></li> <li>RPC method: <code>Statuses</code></li> <li>request schema: <code>google.protobuf.Empty</code></li> <li>response schema: <code>list[IntChoiceSchema]</code></li> </ul> <p>For <code>TextChoices</code>, the generated response uses <code>TextChoiceSchema</code>.</p> <p>Available config fields:</p> <ul> <li><code>name</code>: public RPC method name</li> <li><code>source</code>: a Django choices class (or another object exposing <code>.choices</code>)</li> <li><code>description</code>: optional RPC description</li> <li><code>permissions</code>: optional method-level permissions</li> <li><code>response_schema</code>: optional custom schema override</li> </ul>"},{"location":"model-service/overview/#permission-semantics","title":"Permission Semantics","text":"<ul> <li>if <code>permissions</code> is omitted on <code>ChoiceEndpointConfig</code>, the generated RPC inherits service-level permissions</li> <li>if <code>permissions=[...]</code> is provided, it overrides service-level permissions for that generated RPC</li> </ul> <p>Example: make one choice endpoint public while keeping the rest of the service protected:</p> <pre><code>choice_endpoints=[\n    ChoiceEndpointConfig(\n        name=\"Statuses\",\n        source=ProductStatus,\n        permissions=[AllowAny],\n    ),\n]\n</code></pre>"},{"location":"model-service/overview/#data-helper","title":"Data Helper","text":"<p>You can replace storage logic with custom helper:</p> <pre><code>from grpc_extra import ModelDataHelper\n\n\nclass CustomDataHelper(ModelDataHelper):\n    def list_objects(self, request):\n        return Product.objects.filter(is_deleted=False)\n\n    def get_object(self, request):\n        return Product.objects.get(pk=request.id, is_deleted=False)\n\n    def create_object(self, request):\n        return Product.objects.create(**request.model_dump())\n\n    def update_object(self, request):\n        ...\n\n    def patch_object(self, request):\n        ...\n\n    def delete_object(self, request):\n        ...\n</code></pre> <p>Attach it:</p> <pre><code>@grpc_service(app_label=\"products\", package=\"products\")\nclass ProductService(ModelService):\n    data_helper_class = CustomDataHelper\n    config = ModelServiceConfig(...)\n</code></pre>"},{"location":"model-service/overview/#error-semantics","title":"Error Semantics","text":"<ul> <li>Missing object in <code>Detail</code> -&gt; <code>NOT_FOUND</code></li> <li>Validation problems -&gt; <code>INVALID_ARGUMENT</code></li> <li>Permission denial -&gt; <code>PERMISSION_DENIED</code></li> </ul>"},{"location":"model-service/overview/#permissions-in-modelservice","title":"Permissions in ModelService","text":"<p>Permissions are declared the same way as regular services.</p> <ul> <li>service-level permissions via <code>@grpc_service(..., permissions=[...])</code></li> <li>method-level permissions via generated method metadata or custom methods</li> <li>method-level permissions override service-level permissions when declared explicitly</li> </ul> <p>For <code>Detail</code>/<code>Get</code>, object-level checks are applied on both method-level and service-level permissions only when the method does not override service-level permissions.</p>"},{"location":"model-service/overview/#tips-before-production","title":"Tips Before Production","text":"<ul> <li>Start with <code>LIST + DETAIL</code> and stable schemas first.</li> <li>Always define explicit <code>list_search_fields</code> and <code>list_ordering_fields</code>.</li> <li>Use <code>queryset</code>/<code>detail_queryset</code> from day one to avoid performance regressions.</li> <li>Keep schema field types proto-compatible (<code>Decimal</code> maps to proto <code>string</code>).</li> </ul>"},{"location":"model-service/pagination/","title":"Model Service Pagination","text":"<p>Pagination controls how <code>List</code> responses are sliced and wrapped with metadata.</p> <p>In <code>django-grpc-extra</code> pagination can be applied in two places:</p> <ul> <li><code>ModelService</code> (<code>list_pagination_class</code>)</li> <li>regular service methods via <code>@grpc_pagination</code></li> </ul>"},{"location":"model-service/pagination/#where-it-applies","title":"Where It Applies","text":"<p>For <code>ModelService</code>:</p> <ul> <li><code>List</code> (unary) -&gt; pagination supported</li> <li><code>StreamList</code> (server streaming) -&gt; pagination is not applied</li> </ul> <p>List pipeline order:</p> <ol> <li>filtering</li> <li>searching</li> <li>ordering</li> <li>pagination</li> </ol> <p>Pagination always runs last, so pages are built from already filtered/ordered data.</p>"},{"location":"model-service/pagination/#default-pagination-limitoffsetpagination","title":"Default Pagination: <code>LimitOffsetPagination</code>","text":"<p>Default class is <code>LimitOffsetPagination</code>.</p> <p>It extends request schema with:</p> <ul> <li><code>limit</code> (<code>default=100</code>, <code>ge=1</code>, <code>le=1000</code>)</li> <li><code>offset</code> (<code>default=0</code>, <code>ge=0</code>)</li> </ul> <p>It wraps response schema as:</p> <ul> <li><code>count</code>: total matched rows/items</li> <li><code>limit</code>: requested limit</li> <li><code>offset</code>: requested offset</li> <li><code>results</code>: repeated item schema</li> </ul> <p>Example request:</p> <pre><code>{\n  \"limit\": 20,\n  \"offset\": 40\n}\n</code></pre> <p>Example response:</p> <pre><code>{\n  \"count\": 312,\n  \"limit\": 20,\n  \"offset\": 40,\n  \"results\": [\n    {\"id\": 41, \"name\": \"...\"},\n    {\"id\": 42, \"name\": \"...\"}\n  ]\n}\n</code></pre>"},{"location":"model-service/pagination/#modelservice-usage","title":"ModelService Usage","text":"<pre><code>from grpc_extra import (\n    AllowedEndpoints,\n    LimitOffsetPagination,\n    ModelService,\n    ModelServiceConfig,\n    grpc_service,\n)\n\n\n@grpc_service(app_label=\"inventory\", package=\"inventory\")\nclass ProductService(ModelService):\n    config = ModelServiceConfig(\n        model=Product,\n        allowed_endpoints=[AllowedEndpoints.LIST],\n        list_schema=ProductOut,\n        list_pagination_class=LimitOffsetPagination,\n    )\n</code></pre> <p>If <code>list_pagination_class</code> is omitted, framework uses <code>DEFAULT_PAGINATION_CLASS</code> from settings.</p>"},{"location":"model-service/pagination/#disable-pagination-for-list","title":"Disable pagination for List","text":"<pre><code>config = ModelServiceConfig(\n    ...,\n    list_pagination_class=None,\n)\n</code></pre>"},{"location":"model-service/pagination/#regular-service-usage-grpc_pagination","title":"Regular Service Usage: <code>@grpc_pagination</code>","text":"<pre><code>from grpc_extra import grpc_method, grpc_pagination\n\n\n@grpc_method(request_schema=None, response_schema=ProductOut)\n@grpc_pagination\n# also valid: @grpc_pagination()\ndef list_products(self, request, context):\n    return Product.objects.all()\n</code></pre> <p>Supported forms:</p> <ul> <li><code>@grpc_pagination</code></li> <li><code>@grpc_pagination()</code></li> <li><code>@grpc_pagination(CustomPagination)</code></li> </ul> <p>Constraints:</p> <ul> <li>unary methods only</li> <li>must be used with <code>@grpc_method</code></li> <li><code>response_schema</code> must be set</li> </ul>"},{"location":"model-service/pagination/#proto-contract-what-clients-see","title":"Proto Contract (What Clients See)","text":"<p>For limit/offset pagination, generated proto request contains <code>limit</code>/<code>offset</code> fields and response contains <code>results</code> + metadata fields.</p> <p>This means client code should treat paginated endpoints as object responses, not plain repeated top-level arrays.</p> <p>If you change pagination class later, proto contract changes. Regenerate stubs and update clients.</p>"},{"location":"model-service/pagination/#global-default-pagination-class","title":"Global Default Pagination Class","text":"<pre><code>GRPC_EXTRA = {\n    \"DEFAULT_PAGINATION_CLASS\": \"grpc_extra.pagination.LimitOffsetPagination\",\n}\n</code></pre> <p>Used by:</p> <ul> <li><code>ModelService</code> list endpoints (if per-service class is not provided)</li> <li><code>@grpc_pagination</code> without explicit class</li> </ul>"},{"location":"model-service/pagination/#custom-pagination-class","title":"Custom Pagination Class","text":"<p>Create a class inheriting <code>BasePagination</code> and implement three methods:</p> <ul> <li><code>build_request_schema(request_schema)</code></li> <li><code>build_response_schema(response_schema)</code></li> <li><code>paginate(result, request)</code></li> </ul> <p>Example:</p> <pre><code>from pydantic import BaseModel, Field, create_model\nfrom grpc_extra.pagination import BasePagination\n\n\nclass PageNumberPagination(BasePagination):\n    @classmethod\n    def build_request_schema(cls, request_schema: type[BaseModel] | None) -&gt; type[BaseModel]:\n        name = f\"{request_schema.__name__}WithPage\" if request_schema else \"PageRequest\"\n        if request_schema is None:\n            return create_model(\n                name,\n                page=(int, Field(default=1, ge=1)),\n                page_size=(int, Field(default=20, ge=1, le=200)),\n            )\n        return create_model(\n            name,\n            __base__=request_schema,\n            page=(int, Field(default=1, ge=1)),\n            page_size=(int, Field(default=20, ge=1, le=200)),\n        )\n\n    @classmethod\n    def build_response_schema(cls, response_schema: type[BaseModel]) -&gt; type[BaseModel]:\n        return create_model(\n            f\"{response_schema.__name__}Page\",\n            total=(int, ...),\n            page=(int, ...),\n            page_size=(int, ...),\n            items=(list[response_schema], ...),\n        )\n\n    @classmethod\n    def paginate(cls, result, request: BaseModel) -&gt; dict:\n        # Must return data matching build_response_schema.\n        ...\n</code></pre> <p>Attach:</p> <ul> <li>per-service: <code>list_pagination_class=PageNumberPagination</code></li> <li>per-method: <code>@grpc_pagination(PageNumberPagination)</code></li> </ul>"},{"location":"model-service/pagination/#queryset-vs-list-behavior","title":"QuerySet vs List Behavior","text":"<p>Pagination supports both:</p> <ul> <li><code>QuerySet</code>: efficient <code>count()</code> and slicing</li> <li>Python <code>list</code> / iterable: in-memory slicing</li> </ul> <p>For large datasets, return <code>QuerySet</code> whenever possible.</p>"},{"location":"model-service/pagination/#interaction-with-searching-and-ordering","title":"Interaction with Searching and Ordering","text":"<p>Because pagination is final stage:</p> <ul> <li>search/order changes directly affect page boundaries</li> <li>unstable ordering leads to unstable pages</li> </ul> <p>Recommendations:</p> <ol> <li>always configure explicit ordering fields</li> <li>sort by indexed and deterministic fields</li> <li>avoid non-deterministic ordering for paginated endpoints</li> </ol>"},{"location":"model-service/pagination/#common-errors","title":"Common Errors","text":""},{"location":"model-service/pagination/#invalid_argument-for-limitoffset","title":"<code>INVALID_ARGUMENT</code> for <code>limit</code>/<code>offset</code>","text":"<p>Triggered by request validation, for example:</p> <ul> <li><code>limit &lt;= 0</code></li> <li><code>offset &lt; 0</code></li> <li>wrong type</li> </ul>"},{"location":"model-service/pagination/#pagination-configured-on-streaming-endpoint","title":"Pagination configured on streaming endpoint","text":"<p>Pagination wrappers are unary-oriented and not intended for streaming responses.</p>"},{"location":"model-service/pagination/#client-parse-errors-after-schema-changes","title":"Client parse errors after schema changes","text":"<p>Typical root cause: server proto updated, client stubs stale.</p> <p>Fix sequence:</p> <ol> <li>regenerate proto and stubs</li> <li>restart server</li> <li>refresh API client descriptor/cache</li> </ol>"},{"location":"model-service/pagination/#stability-recommendations","title":"Stability Recommendations","text":"<ol> <li>Keep one response envelope style per endpoint (do not switch often).</li> <li>Keep max page size conservative.</li> <li>Prefer additive changes over renaming/removing pagination fields.</li> <li>Document defaults (<code>limit</code>, <code>offset</code>) for client teams.</li> <li>Add tests for first page, middle page, empty tail page, invalid bounds.</li> </ol>"},{"location":"model-service/searching-ordering/","title":"Model Service Searching and Ordering","text":"<p>This section explains how <code>ModelService</code> list endpoints apply searching and ordering.</p>"},{"location":"model-service/searching-ordering/#where-it-is-configured","title":"Where It Is Configured","text":"<p>In <code>ModelServiceConfig</code>:</p> <ul> <li><code>list_searching_class</code></li> <li><code>list_search_fields</code></li> <li><code>list_ordering_class</code></li> <li><code>list_ordering_fields</code></li> </ul> <p>Example:</p> <pre><code>from grpc_extra import (\n    AllowedEndpoints,\n    ModelService,\n    ModelServiceConfig,\n    Ordering,\n    Searching,\n    grpc_service,\n)\n\n\n@grpc_service(app_label=\"products\", package=\"products\")\nclass ProductService(ModelService):\n    config = ModelServiceConfig(\n        model=Product,\n        allowed_endpoints=[AllowedEndpoints.LIST, AllowedEndpoints.STREAM_LIST],\n        list_schema=ProductOut,\n        list_filter=ProductFilter,\n        list_searching_class=Searching,\n        list_search_fields=[\"sku\", \"name\", \"description\"],\n        list_ordering_class=Ordering,\n        list_ordering_fields=[\"sku\", \"name\", \"created_at\"],\n    )\n</code></pre>"},{"location":"model-service/searching-ordering/#runtime-pipeline-order","title":"Runtime Pipeline Order","text":"<p>For <code>List</code> and <code>StreamList</code>:</p> <ol> <li>filtering (<code>list_filter</code>)</li> <li>searching</li> <li>ordering</li> <li>pagination (<code>List</code> only, if enabled)</li> </ol> <p>This means search/order work on already filtered data.</p>"},{"location":"model-service/searching-ordering/#searching","title":"Searching","text":"<p><code>Searching</code> adds request field:</p> <pre><code>{\"search\": \"foo\"}\n</code></pre> <p>and applies conditions over configured <code>list_search_fields</code>.</p>"},{"location":"model-service/searching-ordering/#search-field-lookup-prefixes","title":"Search field lookup prefixes","text":"<p>Supported prefixes:</p> <ul> <li><code>^field</code> -&gt; <code>istartswith</code></li> <li><code>=field</code> -&gt; <code>iexact</code></li> <li><code>@field</code> -&gt; full-text <code>search</code></li> <li><code>$field</code> -&gt; <code>iregex</code></li> <li><code>field</code> (no prefix) -&gt; <code>icontains</code></li> </ul> <p>Example config:</p> <pre><code>list_search_fields=[\n    \"=sku\",  # exact\n    \"name\",            # icontains\n    \"^category_name\",  # istartswith\n]\n</code></pre>"},{"location":"model-service/searching-ordering/#queryset-behavior","title":"QuerySet behavior","text":"<p>For querysets, search builds <code>Q</code> conditions and applies <code>.filter(...)</code>.</p> <p>Multi-term search semantics:</p> <ul> <li>terms are split by spaces/commas</li> <li>each term must match at least one search field</li> <li>all terms must match (AND between terms)</li> </ul>"},{"location":"model-service/searching-ordering/#listdict-behavior","title":"List/dict behavior","text":"<p>For list-like payloads, search is applied in-memory against dict keys/object attrs.</p> <p>If field is missing in list item, <code>SearchingError</code> is raised.</p>"},{"location":"model-service/searching-ordering/#ordering","title":"Ordering","text":"<p><code>Ordering</code> adds request field:</p> <pre><code>{\"ordering\": \"name,-created_at\"}\n</code></pre>"},{"location":"model-service/searching-ordering/#field-syntax","title":"Field syntax","text":"<ul> <li><code>name</code> -&gt; ascending</li> <li><code>-name</code> -&gt; descending</li> <li>multiple terms are comma-separated</li> </ul>"},{"location":"model-service/searching-ordering/#queryset-behavior_1","title":"QuerySet behavior","text":"<p>For querysets ordering uses <code>.order_by(*terms)</code>.</p>"},{"location":"model-service/searching-ordering/#listdict-behavior_1","title":"List/dict behavior","text":"<p>For in-memory lists, sorting is applied with <code>itemgetter</code>/<code>attrgetter</code>.</p> <p>If field is missing on list item, <code>OrderingError</code> is raised.</p>"},{"location":"model-service/searching-ordering/#field-validation-rules","title":"Field Validation Rules","text":"<p>Both searching and ordering validate against configured fields.</p>"},{"location":"model-service/searching-ordering/#explicit-fields-are-recommended","title":"Explicit fields are recommended","text":"<p><code>ModelServiceConfig</code> should set explicit field lists:</p> <ul> <li><code>list_search_fields=[...]</code></li> <li><code>list_ordering_fields=[...]</code></li> </ul> <p>This keeps behavior predictable and prevents accidental exposure of unintended fields.</p>"},{"location":"model-service/searching-ordering/#__all__-for-ordering","title":"<code>__all__</code> for ordering","text":"<p>Ordering supports <code>\"__all__\"</code> in <code>Ordering</code> class behavior, but explicit lists are safer for stable API contracts.</p>"},{"location":"model-service/searching-ordering/#request-examples","title":"Request Examples","text":""},{"location":"model-service/searching-ordering/#search-only","title":"Search only","text":"<pre><code>{\n  \"search\": \"ACME\"\n}\n</code></pre>"},{"location":"model-service/searching-ordering/#order-only","title":"Order only","text":"<pre><code>{\n  \"ordering\": \"-sku\"\n}\n</code></pre>"},{"location":"model-service/searching-ordering/#search-order-pagination","title":"Search + order + pagination","text":"<pre><code>{\n  \"search\": \"laptop\",\n  \"ordering\": \"name,-sku\",\n  \"limit\": 50,\n  \"offset\": 0\n}\n</code></pre>"},{"location":"model-service/searching-ordering/#common-errors","title":"Common Errors","text":""},{"location":"model-service/searching-ordering/#orderingerror-invalid-ordering-fields","title":"<code>OrderingError: Invalid ordering fields</code>","text":"<p>Cause:</p> <ul> <li>requested field is not in allowed ordering fields</li> </ul> <p>Fix:</p> <ol> <li>add field to <code>list_ordering_fields</code></li> <li>or change request payload to allowed fields</li> </ol>"},{"location":"model-service/searching-ordering/#search-returns-full-list","title":"Search returns full list","text":"<p>Possible causes:</p> <ul> <li>search field list is empty</li> <li>search term is empty/whitespace</li> <li>custom searching class returns input unchanged</li> </ul> <p>Fix:</p> <ol> <li>set explicit <code>list_search_fields</code></li> <li>verify request has non-empty <code>search</code></li> <li>verify custom class implementation</li> </ol>"},{"location":"model-service/searching-ordering/#searchingerror-for-list-items","title":"<code>SearchingError</code> for list items","text":"<p>Cause:</p> <ul> <li>configured search field missing in dict/object items</li> </ul> <p>Fix:</p> <ul> <li>align list item shape with <code>list_search_fields</code></li> </ul>"},{"location":"model-service/searching-ordering/#custom-searchingordering-classes","title":"Custom searching/ordering classes","text":"<p>You can replace default classes:</p> <pre><code>config = ModelServiceConfig(\n    ...,\n    list_searching_class=CustomSearching,\n    list_search_fields=[\"field_a\", \"field_b\"],\n    list_ordering_class=CustomOrdering,\n    list_ordering_fields=[\"field_a\"],\n)\n</code></pre> <p>Keep contract compatibility with base classes:</p> <ul> <li>class should expose <code>build_request_schema(...)</code></li> <li>instance should implement <code>search(...)</code> or <code>order(...)</code></li> </ul>"},{"location":"model-service/searching-ordering/#best-practices","title":"Best Practices","text":"<ol> <li>Keep <code>list_search_fields</code> and <code>list_ordering_fields</code> explicit and minimal.</li> <li>Use indexed DB fields first for ordering/searching when possible.</li> <li>Avoid expensive regex/full-text lookups unless needed.</li> <li>Validate payloads in client SDK/tooling to reduce invalid requests.</li> <li>For high-volume endpoints, combine optimized queryset + explicit fields + pagination.</li> </ol>"},{"location":"reference/errors/","title":"Error Mapping","text":"<p>This page documents default runtime exception-to-gRPC status conversion and practical debugging guidance.</p>"},{"location":"reference/errors/#default-mapping-table","title":"Default Mapping Table","text":"Exception type gRPC status Typical cause <code>pydantic.ValidationError</code> <code>INVALID_ARGUMENT</code> request payload doesn't match schema <code>RequestDecodeError</code> with validation cause <code>INVALID_ARGUMENT</code> pb2 request decoded to invalid Pydantic payload <code>PermissionError</code> <code>PERMISSION_DENIED</code> permission check failed <code>django.core.exceptions.ObjectDoesNotExist</code> <code>NOT_FOUND</code> <code>Detail/Get</code> object is missing <code>OrderingError</code> / <code>SearchingError</code> <code>INVALID_ARGUMENT</code> invalid ordering/search field/value <code>RequestDecodeError</code> / <code>ResponseEncodeError</code> <code>INTERNAL</code> runtime conversion failure any other exception <code>UNKNOWN</code> unhandled application/runtime error"},{"location":"reference/errors/#where-mapping-happens","title":"Where Mapping Happens","text":"<p>Runtime catches exceptions inside method wrappers and aborts gRPC context with mapped status + message.</p> <p>This behavior applies to unary and streaming wrappers.</p>"},{"location":"reference/errors/#common-error-patterns","title":"Common Error Patterns","text":""},{"location":"reference/errors/#invalid_argument","title":"<code>INVALID_ARGUMENT</code>","text":"<p>Most common triggers:</p> <ul> <li>missing required request fields</li> <li>wrong field types (<code>str</code> instead of <code>int</code>, invalid enum value)</li> <li>malformed search/order values</li> </ul> <p>What to check:</p> <ol> <li>request payload against generated proto schema</li> <li>request Pydantic schema defaults/constraints</li> <li>decorator-provided request augmentation (<code>search</code>, <code>ordering</code>, <code>limit</code>, <code>offset</code>)</li> </ol>"},{"location":"reference/errors/#permission_denied","title":"<code>PERMISSION_DENIED</code>","text":"<p>Most common triggers:</p> <ul> <li><code>has_perm</code> returned <code>False</code></li> <li><code>has_obj_perm</code> returned <code>False</code></li> </ul> <p>What to check:</p> <ol> <li>whether method-level permissions intentionally override service-level permissions</li> <li>identity attached to <code>context</code> in auth backend (<code>context.user</code>)</li> <li>object-level checks for <code>Detail/Get</code></li> </ol>"},{"location":"reference/errors/#unauthenticated","title":"<code>UNAUTHENTICATED</code>","text":"<p>Most common triggers:</p> <ul> <li>auth backend returned <code>None</code> or <code>False</code></li> <li>token extraction failed (header/scheme/value format)</li> </ul> <p>What to check:</p> <ol> <li>metadata key/value in client call</li> <li>backend <code>scheme</code>/<code>header</code> configuration</li> <li>whitelist logic for public RPC methods (e.g. health)</li> </ol>"},{"location":"reference/errors/#not_found","title":"<code>NOT_FOUND</code>","text":"<p>Most common trigger:</p> <ul> <li>object lookup in detail endpoint fails</li> </ul> <p>What to check:</p> <ol> <li>lookup field (<code>id</code>, external key, etc.)</li> <li>queryset filters applied by service/data helper</li> </ol>"},{"location":"reference/errors/#internal","title":"<code>INTERNAL</code>","text":"<p>Most common triggers:</p> <ul> <li>response payload doesn't match <code>response_schema</code></li> <li>unsupported payload shape for pb2 constructor</li> </ul> <p>What to check:</p> <ol> <li>actual method return type</li> <li>nested schema field types</li> <li>decimal/date/time conversions and proto field types</li> </ol>"},{"location":"reference/errors/#unknown","title":"<code>UNKNOWN</code>","text":"<p>Means exception wasn't matched by default mapper.</p> <p>What to check:</p> <ol> <li>server logs with stack trace</li> <li>add custom mapper to classify domain exceptions</li> </ol>"},{"location":"reference/errors/#custom-exception-mapper","title":"Custom Exception Mapper","text":"<p>You can override mapping globally:</p> <pre><code>from grpc_extra.exceptions import MappedError\nimport grpc\n\n\ndef custom_exception_mapper(exc: Exception) -&gt; MappedError:\n    if isinstance(exc, DomainConflictError):\n        return MappedError(grpc.StatusCode.ALREADY_EXISTS, str(exc))\n    return MappedError(grpc.StatusCode.UNKNOWN, str(exc))\n\n\nGRPC_EXTRA = {\n    \"EXCEPTION_MAPPER\": custom_exception_mapper,\n    # or import path string\n    # \"EXCEPTION_MAPPER\": \"path.to.custom_exception_mapper\",\n}\n</code></pre> <p>Mapper must be callable; class types are not accepted.</p>"},{"location":"reference/errors/#debugging-checklist","title":"Debugging Checklist","text":"<p>When status is unexpected:</p> <ol> <li>reproduce with minimal payload</li> <li>check generated proto is up to date</li> <li>restart server</li> <li>recreate client reflection/descriptor cache</li> <li>inspect server logs with full traceback</li> <li>verify auth + permissions path before business logic</li> </ol>"},{"location":"reference/settings/","title":"Settings Reference","text":"<p>Runtime is configured through <code>GRPC_EXTRA</code> in Django settings.</p> <pre><code>GRPC_EXTRA = {\n    \"BIND\": \"[::]:50051\",\n    \"MAX_WORKERS\": 10,\n    \"MAX_MSG_MB\": 32,\n    \"ENABLE_HEALTH\": True,\n    \"ENABLE_REFLECTION\": False,\n    \"AUTH_BACKEND\": \"path.to.auth_backend\",\n    \"INTERCEPTORS\": [],\n    \"EXCEPTION_MAPPER\": \"path.to.exception_mapper\",\n    \"ENABLE_REQUEST_LOGGING\": True,\n    \"LOGGER_NAME\": \"grpc_extra\",\n    \"DEFAULT_PAGINATION_CLASS\": \"grpc_extra.pagination.LimitOffsetPagination\",\n    \"ENABLE_RELOAD\": False,\n    \"RELOAD_PATHS\": (\".\",),\n\n    # proto naming customization\n    \"SCHEMA_SUFFIX_STRIP\": (\"Schema\",),\n    \"REQUEST_SUFFIX\": \"Request\",\n    \"RESPONSE_SUFFIX\": \"Response\",\n\n    # optional SDK generator mapping (internal/advanced)\n    \"SDK_GENERATORS\": {\n        # \"python\": \"path.to.CustomPythonGenerator\",\n    },\n\n    # required for PHP generator when used\n    \"PHP_GRPC_PLUGIN\": \"/usr/local/bin/grpc_php_plugin\",\n}\n</code></pre>"},{"location":"reference/settings/#core-runtime-keys","title":"Core Runtime Keys","text":""},{"location":"reference/settings/#bind","title":"<code>BIND</code>","text":"<p>Server bind address.</p> <ul> <li>type: <code>str</code></li> <li>default: <code>\"[::]:50051\"</code></li> </ul> <p>Examples:</p> <ul> <li><code>\"0.0.0.0:50051\"</code></li> <li><code>\"127.0.0.1:50051\"</code></li> </ul>"},{"location":"reference/settings/#max_workers","title":"<code>MAX_WORKERS</code>","text":"<p>ThreadPool workers for gRPC server.</p> <ul> <li>type: <code>int</code></li> <li>default: <code>10</code></li> </ul>"},{"location":"reference/settings/#max_msg_mb","title":"<code>MAX_MSG_MB</code>","text":"<p>Max send/receive message size in MB.</p> <ul> <li>type: <code>int</code></li> <li>default: <code>32</code></li> </ul>"},{"location":"reference/settings/#feature-toggles","title":"Feature Toggles","text":""},{"location":"reference/settings/#enable_health","title":"<code>ENABLE_HEALTH</code>","text":"<p>Enable gRPC health service.</p> <ul> <li>type: <code>bool</code></li> <li>default: <code>True</code></li> </ul>"},{"location":"reference/settings/#enable_reflection","title":"<code>ENABLE_REFLECTION</code>","text":"<p>Enable gRPC reflection service.</p> <ul> <li>type: <code>bool</code></li> <li>default: <code>False</code></li> </ul>"},{"location":"reference/settings/#enable_request_logging","title":"<code>ENABLE_REQUEST_LOGGING</code>","text":"<p>Enable request logging interceptor.</p> <ul> <li>type: <code>bool</code></li> <li>default: <code>True</code></li> </ul>"},{"location":"reference/settings/#logger_name","title":"<code>LOGGER_NAME</code>","text":"<p>Logger name used for request logs.</p> <ul> <li>type: <code>str</code></li> <li>default: <code>\"grpc_extra\"</code></li> </ul>"},{"location":"reference/settings/#auth-and-error-handling","title":"Auth and Error Handling","text":""},{"location":"reference/settings/#auth_backend","title":"<code>AUTH_BACKEND</code>","text":"<p>Auth backend source.</p> <ul> <li>type: callable | class | import path string | <code>None</code></li> <li>default: <code>None</code></li> </ul> <p>Contract:</p> <pre><code>def auth_backend(context, method: str, request=None) -&gt; bool | object | None:\n    ...\n</code></pre> <p>Semantics:</p> <ul> <li><code>False</code>/<code>None</code> =&gt; <code>UNAUTHENTICATED</code></li> <li>truthy =&gt; allow</li> </ul>"},{"location":"reference/settings/#exception_mapper","title":"<code>EXCEPTION_MAPPER</code>","text":"<p>Custom exception mapper.</p> <ul> <li>type: callable or import path string to callable</li> <li>default: internal mapper</li> </ul> <p>Contract:</p> <pre><code>from grpc_extra.exceptions import MappedError\n\n\ndef exception_mapper(exc: Exception) -&gt; MappedError:\n    ...\n</code></pre> <p>Class types are not accepted as mapper targets.</p>"},{"location":"reference/settings/#interceptors","title":"Interceptors","text":""},{"location":"reference/settings/#interceptors_1","title":"<code>INTERCEPTORS</code>","text":"<p>Extra server interceptors.</p> <ul> <li>type: iterable of import paths / interceptor objects</li> <li>default: empty</li> </ul> <p>In reload mode, prefer import paths instead of runtime object instances.</p>"},{"location":"reference/settings/#pagination-defaults","title":"Pagination Defaults","text":""},{"location":"reference/settings/#default_pagination_class","title":"<code>DEFAULT_PAGINATION_CLASS</code>","text":"<p>Default pagination class for:</p> <ul> <li><code>@grpc_pagination()</code></li> <li> <p><code>ModelService</code> list endpoint</p> </li> <li> <p>type: import path string | class | <code>None</code></p> </li> <li>default: <code>\"grpc_extra.pagination.LimitOffsetPagination\"</code></li> </ul>"},{"location":"reference/settings/#reload-settings","title":"Reload Settings","text":""},{"location":"reference/settings/#enable_reload","title":"<code>ENABLE_RELOAD</code>","text":"<p>Enable live reload mode.</p> <ul> <li>type: <code>bool</code></li> <li>default: <code>False</code></li> </ul>"},{"location":"reference/settings/#reload_paths","title":"<code>RELOAD_PATHS</code>","text":"<p>Watched paths for reload mode.</p> <ul> <li>type: iterable[str]</li> <li>default: <code>( \".\", )</code></li> </ul> <p>Use uppercase key <code>RELOAD_PATHS</code>.</p>"},{"location":"reference/settings/#proto-naming-settings","title":"Proto Naming Settings","text":""},{"location":"reference/settings/#schema_suffix_strip","title":"<code>SCHEMA_SUFFIX_STRIP</code>","text":"<p>Suffixes stripped from Pydantic schema names before request/response message naming.</p> <ul> <li>type: <code>tuple[str, ...] | str</code></li> <li>default: <code>( \"Schema\", )</code></li> </ul>"},{"location":"reference/settings/#request_suffix","title":"<code>REQUEST_SUFFIX</code>","text":"<p>Suffix for generated request messages.</p> <ul> <li>type: <code>str</code></li> <li>default: <code>\"Request\"</code></li> </ul>"},{"location":"reference/settings/#response_suffix","title":"<code>RESPONSE_SUFFIX</code>","text":"<p>Suffix for generated response messages.</p> <ul> <li>type: <code>str</code></li> <li>default: <code>\"Response\"</code></li> </ul>"},{"location":"reference/settings/#optional-advanced-keys","title":"Optional Advanced Keys","text":""},{"location":"reference/settings/#sdk_generators","title":"<code>SDK_GENERATORS</code>","text":"<p>Custom SDK generator mapping (advanced/internal use).</p> <ul> <li>type: <code>dict[str, str]</code></li> <li>default: internal mapping</li> </ul>"},{"location":"reference/settings/#php_grpc_plugin","title":"<code>PHP_GRPC_PLUGIN</code>","text":"<p>Path to <code>grpc_php_plugin</code> binary for PHP generation.</p> <ul> <li>type: <code>str</code></li> <li>required only if PHP generation is used</li> </ul>"},{"location":"reference/settings/#recommended-baseline-dev","title":"Recommended Baseline (Dev)","text":"<pre><code>GRPC_EXTRA = {\n    \"BIND\": \"0.0.0.0:50051\",\n    \"ENABLE_HEALTH\": True,\n    \"ENABLE_REFLECTION\": True,\n    \"ENABLE_REQUEST_LOGGING\": True,\n    \"ENABLE_RELOAD\": True,\n    \"RELOAD_PATHS\": (\"app\",),\n}\n</code></pre>"},{"location":"reference/settings/#recommended-baseline-prod","title":"Recommended Baseline (Prod)","text":"<pre><code>GRPC_EXTRA = {\n    \"BIND\": \"0.0.0.0:50051\",\n    \"ENABLE_HEALTH\": True,\n    \"ENABLE_REFLECTION\": False,\n    \"ENABLE_REQUEST_LOGGING\": True,\n    \"MAX_MSG_MB\": 32,\n    \"AUTH_BACKEND\": \"path.to.auth.GrpcBearerAuth\",\n    \"ENABLE_RELOAD\": False,\n}\n</code></pre>"}]}